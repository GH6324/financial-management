-- =========================================================
-- V28 · v0.5 · 股票持仓现金联动标记(2026-06-01)· FR-78/79
-- =========================================================
-- 录入 AUTO 持仓时可选「从账户现金划转买入」:买入扣现金 / 卖出(归档)按市价加回。
-- cash_linked = 1 标记该持仓由现金划转买入 → 归档时才对称把现金加回(向后兼容:
-- 老持仓 cash_linked=0 → 归档不动现金,保持 v0.3/v0.4 行为不变)。
--
-- backward-compat:纯 ADD COLUMN DEFAULT 0 · 老数据全部视为非联动 · 0 风险。
-- =========================================================

ALTER TABLE stock_holding
  ADD COLUMN cash_linked TINYINT(1) NOT NULL DEFAULT 0 AFTER manual_value_at;
