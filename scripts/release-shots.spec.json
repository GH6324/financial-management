{
 "_comment": "每次发版按核心能力改清单(PC 用 selector 裁剪聚焦;手机不裁剪、整屏 full=false 平铺 —— 用户定 2026-07-11)",
 "_version": "v1.17.0 · Docker 下富途 OpenD 一键接入(可选网关容器)。核心 UI 是券商向导页的两种形态:① 未启用时的「你正在引入什么」公示块(要在带共享卷的实例上才看得到,beta 是 systemd 原生,所以这张走 SHOT_BASE=20099 的临时实例);② 已可安装时的第 1 步(版本号不再要用户填)。两张都不含金额。",
 "pc": [
  {
   "path": "/admin/broker/opend",
   "name": "futu-install-step",
   "wait": 1500,
   "waitFor": "text=第 1 步",
   "selector": "#sec1"
  }
 ],
 "mobile": [
  {
   "path": "/admin/broker/opend",
   "name": "futu-install-step",
   "wait": 1500,
   "full": false,
   "waitFor": "text=第 1 步",
   "selector": "#sec1"
  }
 ]
}