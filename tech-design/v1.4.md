# Tech-Design · v1.4 · 持仓截图智能解析

> **状态**:草案 · 待评审(承 `prd/v1.4.md`)· 视觉链路已真机 spike 验证
> **范围**:让 WEALTH/CASH(基金/理财/支付宝)大账户支持「截图 → 视觉解析 → 与已有持仓三态对账 → 确认落库」的多真实持仓建模,复用现有持仓 / 估值 / 对账 / 透视全链路,线上历史数据零回溯兼容。

---

## 0 · 现状锚点(探索结论 · file:line)

| 能力 | 落点 | 复用方式 |
|---|---|---|
| 持仓表(通用) | `stock_holding`(`V16`)· `StockHolding.java` | MANUAL 模式 = 名称+manual_value,天然适配基金,**直接复用** |
| 持仓 CRUD | `StockHoldingController`(`/accounts/{id}/holdings/*`)· `StockHoldingService` | 确认表 / 落库复用 create/update/archive |
| 账户估值 + 写回 | `AccountValuationService.valuateInternal / writeBackBalance` | Σ持仓 → 写回 `period_snapshot`;**有持仓才接管、没持仓不碰**(backward-compat 红线) |
| 支持持仓的账户类型 | `AccountValuationService.supportsHoldings()`(STOCK/CRYPTO/METAL) | **需放开到 WEALTH/CASH** |
| 三态对账 | `BrokerSyncService.reconcile()`(新增/更新/归档 · 只动本 `sync_source`) | **算法直接借** |
| 透视装配 | `LensQueryService`(账户拆 N 持仓)+ `PivotEngine`(持仓级维度+诚实降级) | 新持仓落进去,旭日/透视/体检**自动细分,零改** |
| LLM 基建 | `QwenLlmClient`(DashScope OpenAI 兼容 + Bearer key + 故障转移/熔断)· `LlmClient` 接口 | 加多模态变体 |
| 打标白名单 | `LensAiTagService`(AssetClass/IndustryTag `fromName` 校验) | 直接借 |
| 上传安全 | `LogoUploadController`(类型/大小/魔数/路径穿越/审计) | 照搬加固 |
| 前端压缩 | `admin/family.html` logo `canvas.toBlob('image/webp',0.82)` | 复用模式(参数调大) |
| 填报/账期 | `EntryController` + `PeriodService`(OPEN/CLOSED · 关账不可改) | 入口挂进填报;导入只写当前 OPEN 期 |

---

## 1 · 架构决策(备选 + 取舍 + 选定)

### D1 · 数据模型:持仓落在哪张表

| 备选 | 取舍 | |
|---|---|---|
| A. 新建 `fund_holding` 表 | 语义干净,但**复制**估值/CRUD/装配/对账四套逻辑 → 两条平行链路,长期技术债;`LensQueryService`/`AccountValuationService` 都要分叉 | ✗ |
| B. 泛化:`stock_holding` rename 成 `holding` | 语义最正,但**线上表 rename 是高风险迁移**(外键/mapper/历史数据/回滚),只为改名,不值 | ✗ |
| **C. 复用 `stock_holding`(选定)** | 它**本就是通用持仓表**(MANUAL=名称+manual_value 无需 ticker);复用全链路;仅加 3 处向后兼容小改 | ✓ |

**选 C**。代价诚实说:表名 `stock_holding` 对基金语义不够贴切(命名债),但 rename 的迁移风险远大于收益,记为已知债、不动。改动面:
1. `AccountValuationService.supportsHoldings()`:`STOCK/CRYPTO/METAL` → 追加 `WEALTH/CASH`。
2. 加**可空**列 `asset_class_tag`(`VARCHAR(20)`)、`risk_tag`(`VARCHAR(16)`)、`liquidity_tag`(`VARCHAR(16)`);持仓级为空时**回落账户级**同名标签(老 STOCK 持仓全空 → 行为不变)。
3. `sync_source` 允许新值 `SCREENSHOT`(现为 `FUTU/TIGER/NULL`,`VARCHAR(8)` 够放)。

### D2 · 解析技术:视觉大模型 vs OCR(已验证)

选 **`qwen-vl-max`(视觉大模型)**,承 PRD §4。真机 spike 实测:3 张真实截图、~18 支持仓、**市值 100% 对**、自动跳过汇总行、截断行 `confidence:low` 诚实标疑、~10s/张。传统 OCR「文本汤」跨 app 拼行极脆,前端 OCR 手机慢+中文差,均 pass。**关键复用**:同一 DashScope OpenAI 兼容端点 + 现有 Qwen key,零新供应商。

### D3 · 对账 diff:复用券商 reconcile vs 自造

选 **复用 `BrokerSyncService.reconcile` 三态思路**。它已实现「按键匹配 → 新增 insert / 命中 update / 消失 archive,且**只动指定 `sync_source` 的行**,手填(NULL)绝不误删」——正是所需。自造等于重写已被 `BrokerReconcileTest` 网住的逻辑,无谓。**适配点**见 D4/D5。

### D4 · 匹配键:ticker vs 名称 vs 混合

券商用 `ticker+market`(API 已标准化);基金截图**常无 ticker**。选 **混合键**:
- 若截到**基金代码**(6 位数字,如 `005827`)→ 用代码(强键)。
- 否则用**归一化名称**:去空格 / 全角半角统一 / 去常见噪声后缀(`A/C/(LOF)/(QDII-LOF)/联接` 等按规则保留主体)。归一仅用于**匹配**,展示名保留原文。
- 命中阈值:代码精确等价;名称归一后精确等价(不做模糊近似,避免误并两支不同基金)——**匹配不上就当新增**,由用户在确认表手动合并,比自动误并安全。

### D5 · 卖出 / 消失判定:自动归档 vs 用户定夺

选 **用户定夺(默认保留)**。因为 **FR-1 一账户多图**:用户可能只截了列表的一部分(滚动分段),没截到 ≠ 卖出。
- 一次导入的**所有截图先合并成一个持仓集**,再对该账户 `SCREENSHOT` 行算 diff。
- 「库有、合并集无」的行 → 归到确认表「本次未出现」组,**逐条让用户选 保留 / 归档**;默认保留。绝不自动 archive。

### D6 · 估值交接 & 向后兼容:复用 STOCK writeback(关键)

选 **复用 `AccountValuationService` 既有交接机制**,不发明新迁移:
- **历史期零回溯**:P1…P(n-1) 的 `period_snapshot`(手填单一余额)原样保留 → 该段净值曲线**完全不变**。
- **当期交接**:在当前 OPEN 期导入持仓后,`supportsHoldings(WEALTH/CASH)=true` 使 `AccountValuationService` 接管 → Σ持仓 写回当期 snapshot(note=系统估值)。
- **防跳变**:FR-8 对账让用户把 Σ持仓 对齐到账户当前余额(补现金/其他行)→ 交接当期 Σ持仓 ≈ 实际值 → MoM 无虚高尖峰。
- **红线守恒**:导入前该账户 `holdings.isEmpty()` → 系统从不碰其手填 snapshot;导入后才接管;已关账历史期永不改。
- 为什么不「迁移历史成本基础/冲销差额」:现有 STOCK 转持仓从不这么做,曲线照样连续;引入历史回溯反而破坏「已关账不可改」不变量。**否决**。

### D7 · 前端压缩:canvas vs 第三方库 vs 服务端压

选 **前端 canvas 压缩**(复用 logo 模式,参数调大):长边限 ~2000px、`toBlob('image/jpeg',0.82)`(截图多为照片式,JPEG 比 WebP 兼容更稳;文字在 2000px/0.82 下实测清晰)。
- 不选第三方库(browser-image-compression):多一个 vendor 依赖,canvas 够用。
- 不选纯服务端压:大图先过网络到服务器再压,浪费用户流量 + 撞 nginx/multipart 上限。前端压是省流量的正解。
- 服务端仍**保留大小上限兜底**(单图 ≤8MB、单次 ≤ 图数×8MB),防绕过前端直传。

### D8 · 缺 key / 无视觉时的降级

`QwenVisionClient.available()` = Qwen key 非空。不可用时:整功能入口**优雅禁用 + 文案提示**(承 `LlmClient.available()` 与 LensAiTag 模式),不报错、不半吊子。DeepSeek 无视觉 → 视觉链路**只走 Qwen**,不 failover 到 DeepSeek(与文本诊断的多厂商兜底不同,此处显式限定)。

---

## 2 · 数据模型变更(migration)

新增一个迁移 `V48__holding_screenshot_tags.sql`(编号按现状顺延):

```sql
-- 持仓级资产类型/风险/流动性标签(可空 · 为空回落账户级)· 截图导入的基金逐支打标用
ALTER TABLE stock_holding ADD COLUMN asset_class_tag VARCHAR(20) NULL COMMENT '资产类型(AssetClass.name · 空=回落账户级)';
ALTER TABLE stock_holding ADD COLUMN risk_tag        VARCHAR(16) NULL COMMENT '风险(空=回落账户级)';
ALTER TABLE stock_holding ADD COLUMN liquidity_tag   VARCHAR(16) NULL COMMENT '流动性(空=回落账户级)';
-- sync_source 复用既有列(VARCHAR(8)),新增取值 SCREENSHOT,无需改表结构
```

**backward-compat**:三列全 `NULL` 默认 → 存量 STOCK 持仓读出为 null → 回落账户级 → **零行为变化**。`sync_source` 是加值不改结构。**无破坏性 DDL**(纯 ADD COLUMN NULL)。

---

## 3 · 组件设计

### 3.1 视觉客户端 `QwenVisionClient`(spike 已验证雏形)
- 端点 / 鉴权 / key 与 `QwenLlmClient` 同(`FamilyConfigService.K_LLM_QWEN_KEY`,不碰明文)。
- 模型:`K_LLM_VISION_MODEL`(管理页可配,默认 `qwen-vl-max`)。
- 请求:`messages=[{system:转写规范}, {user: [{type:text,text:PROMPT},{type:image_url,image_url:{url:"data:image/jpeg;base64,…"}}]}]`,`temperature=0.1`。
- Prompt(承 spike + 调优):强约束「只转写屏幕可见、禁计算、禁补全、截断标 low、跳过分类汇总行、市值原样字符串、基金代码若见则填」;输出严格 JSON 数组。
- 解析:抽首个 JSON 数组 → 逐项白名单校验(数值只做格式规整不做运算;标签走 `AssetClass/IndustryTag.fromName`)。
- 故障:复用熔断;失败友好归类(不泄 key)。

### 3.2 导入服务 `HoldingImportService`
```
parse(accountId, List<image>) → List<ParsedHolding>        // 多图并行识别 + 合并去重(按 D4 键)
reconcile(accountId, List<ParsedHolding>) → ReconcilePlan   // 三态:added/changed/missing(复用 reconcile 思路)
                                                            // 只比对该账户 sync_source=SCREENSHOT 的活持仓
apply(accountId, ConfirmedPlan) → void                      // 事务:insert/update/archive(用户确认后)
                                                            // → 触发 AccountValuationService.refresh(写回当期 snapshot)
                                                            // → 发 Lens 缓存失效事件(AFTER_COMMIT)
```
- `ParsedHolding{name, code?, marketValue?, pnl?, confidence, assetClass?, industry?, platform?}`。
- `ReconcilePlan{added[], changed[oldVal→newVal], missing[]}`,金额/Σ/差额工程算好。
- `apply` 全事务;`missing` 仅在用户勾「归档」时 archive,否则不动。

### 3.3 上传端点(加固版,承 LogoUploadController)
- `POST /accounts/{id}/holdings/import`(multipart 多图)· 认证 + CSRF · 类型 image/* + 魔数 + 单图/总量上限 · 存临时 → 识别 → **即删原图** · 审计 `HOLDING_IMPORT`。
- 识别结果进 session/临时,渲染确认表;`POST …/import/confirm` 落库。

### 3.4 前端
- 复用 logo canvas 压缩(长边 2000 / JPEG 0.82)· 多选 + 缩略图 + 删除 + 补图 · 进度态。
- 确认表(移动优先):三态徽记 + 可编辑 + 消失组定夺 + 对账条(承 preview/v1.4)。
- 入口:填报页账户行「截图导入持仓」+ `stock/holdings.html` 顶部按钮。

### 3.5 Lens / 透视
- **零改**:持仓落库 + 缓存失效后,`LensQueryService` 自动把该账户拆 N 持仓,旭日/透视/体检按 industry/assetClass 细分。持仓级标签取 holding 列(空回落账户级)。

---

## 4 · 端到端数据流

```
填报页/账户页「截图导入持仓」
  → 前端 canvas 压缩(长边2000/JPEG0.82)+ 多选
  → POST /accounts/{id}/holdings/import(多图 · CSRF)
  → 校验(类型/魔数/大小) → QwenVisionClient 逐图识别 → 合并去重
  → 白名单打标 → reconcile(与该账户 SCREENSHOT 活持仓比)→ ReconcilePlan(增/改/消失)
  → 原图即删 · 渲染确认表(三态 + 对账条)
  → 用户改/删/加/定夺消失 → POST …/import/confirm
  → 事务 apply(insert/update/archive · sync_source=SCREENSHOT)
  → AccountValuationService.refresh(Σ持仓 写回当前 OPEN 期 snapshot)
  → Lens 缓存失效(AFTER_COMMIT)
  → 回账户详情:N 支持仓;旭日/透视自动细分
```

---

## 5 · Backward-Compat 自检(承 `feedback_prod_backward_compat`)

**Schema 4 检**:
1. 新列全 `NULL` 默认 → 存量行读出 null → 回落账户级 → 零变化。✓
2. 纯 `ADD COLUMN NULL`,无改列/删列/改类型 → 无锁表风险(在线 DDL 友好)。✓
3. `sync_source` 加值不改结构;老值 `FUTU/TIGER/NULL` 语义不变。✓
4. 回滚:migration 只加列,回退 jar 不回 DB → 新列被老代码忽略,无害。✓

**代码 4 检**:
1. `supportsHoldings()` 加 `WEALTH/CASH`:只影响「有持仓」的这类账户;**没导入持仓的存量 WEALTH/CASH 账户 `holdings.isEmpty()→skip`,估值路径完全不变**。✓
2. 持仓级标签回落逻辑:holding 标签为空 → 用账户级(现状),STOCK 老持仓零影响。✓
3. 截图对账**只碰 `sync_source=SCREENSHOT`**:手填 / 券商持仓不受影响(承 reconcile 护栏 + `BrokerReconcileTest` 同类保障)。✓
4. 视觉缺 key → 入口禁用,不影响任何现有功能。✓

**部署红线**:
- 迁移先行(`db/apply.sh`)→ 再上 jar(新列已在)。
- 视觉模型 key 复用现有 Qwen key,无需新配(管理页新增 `K_LLM_VISION_MODEL` 有默认值,不配也能跑)。
- 币种不变量:持仓市值按 FX 缩放入账户本位币,承 `CurrencyInvarianceTest`;比值类指标不受影响。

---

## 6 · 安全 / 隐私

- 上传:认证 + CSRF + 类型/魔数/大小 + 路径穿越防御(承 LogoUploadController)。
- **原图识别后即删**,不长期落盘;只存用户确认的结构化结果。
- 发图给 Qwen 前上传页明示;审计记 `HOLDING_IMPORT`(不记图、不记 key)。
- LLM 只转写不算数(承 `feedback_llm_no_math`);真名不涉及(基金名是公开产品名,非 PII)。

---

## 7 · 测试策略

- **单测**:`HoldingImportService` reconcile 三态(新增/变化/消失)· 归一化匹配键 · 多图合并去重 · 消失默认保留 · `supportsHoldings` 放开后 WEALTH 账户 Σ持仓写回 · 标签回落 · 币种缩放(承 CurrencyInvariance)· `QwenVisionClient` JSON 解析 + 白名单(mock HTTP)。
- **e2e**(`scripts/e2e.sh` 加线):唤起 beta → 上传(mock 或真识别)→ 确认 → DB 出现 N 持仓 → 旭日/透视按 industry 细分 → **历史净值曲线该账户交接前段不变**(断言 backward-compat)。
- **qa 守护**:`v14-HOLDING-IMPORT`(端点 + 视觉 client + 白名单 + 禁算约束 + 确认才落库 + 对账不改额 + supportsHoldings 含 WEALTH/CASH + sync_source=SCREENSHOT 隔离)。
- **真机 UED**:确认表双端截图(承 `feedback_ui_ued_review`);移动优先对齐。
- **文档同步**:`prd/v1.4.md` + 本文件 + `docs/qa-cases.md`(承 `feedback_doc_sync`)。

---

## 8 · 落地顺序(评审通过后)

1. migration `V48`(加列)+ `supportsHoldings` 放开 + 标签回落。
2. `QwenVisionClient`(承 spike 转正)+ prompt 定稿 + 白名单 + 管理页 `K_LLM_VISION_MODEL`。
3. `HoldingImportService`(parse/reconcile/apply)+ 复用 reconcile 三态。
4. 上传端点(加固 + 即删 + 审计)+ 前端压缩/多图。
5. 确认表(三态 + 定夺 + 对账条)+ 填报/账户页入口。
6. 估值交接验证(net worth 曲线不断)+ Lens 细分验证。
7. qa + e2e + 双端 UED + 文档同步 → 发版(`release vX.Y.Z` 闸门)。

---

## 9 · 未决 / 待评审拍板点

- 标签列:只加 `asset_class_tag`,还是连 `risk_tag/liquidity_tag` 一起加?(倾向三个一起,基金风险/流动性差异也大。)
- 启用账户类型:WEALTH + CASH(+ STOCK 已支持);OTHER 是否也放开?
- 消失项默认:保留(倾向)vs 高亮催办。
- Prompt 抓哪个收益列(昨日/持有/累计)——或干脆只抓市值,收益不入库(倾向:只入市值,收益 spike 里发现三列易混)。
