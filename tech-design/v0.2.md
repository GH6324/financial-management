# 家庭账房 · v0.2 技术设计 (TDD)

> 对应 PRD:`prd/v0.2.md`
> 范围(2026-05-10 更新):**v0.2 全部 FR 已完整实施**
>   - 主线 A · 账本视角增强:FR-30 / FR-31 / FR-32 / FR-33 / FR-34 / FR-36 / FR-37 / FR-38
>   - 主线 B · 资产体检模块:FR-40 / FR-40a / FR-40b / FR-40c(含决策 20 AI 综合诊断升级) / FR-40d / FR-40e
>
> 本文档章节 1-9 详述 FR-33/34 的初版方案(2026-05-09 已交付);其它 FR 的详细方案见 `tech-design/v0.2-checkup.md`(决策 1-20),并在本文档 § 11 完整交付清单中索引。
>
> 本文档假设你熟读 v0.1 TDD(`tech-design/v0.1.md`),不再赘述项目骨架、Spring Security 配置、Thymeleaf fragment 体系、buildVersion 缓存策略。

---

## 0. 实施总览

### 0.1 范围声明

| 项 | 是否引入 | 说明 |
|---|---|---|
| 数据库迁移(V10/V11) | ❌ 不引入 | FR-33/34 纯前端 + 静态资源,无任何 SQL DDL |
| 新 Java 类 | ❌ 不引入 | 不写新 Controller / Service / Mapper |
| Java 配置改动 | ✅ 1 处 | `SecurityConfig` 放行 `/manifest.webmanifest` + `/js/**` |
| Thymeleaf 模板改动 | ✅ 1 处 | `templates/fragments/layout.html` head 段 |
| 新增静态资源 | ✅ 5 个文件 | `manifest.webmanifest` + `apple-touch-icon.svg` + 3 个 PNG |
| 新增 JS 脚本 | ✅ 1 个文件 | `static/js/mobile-guide.js`(共 ~150 行) |
| 引入新依赖 | ❌ 0 个 | 不加 Maven 依赖,不加 npm,无 build 工具链改动 |
| 引入新外部服务 | ❌ 0 个 | 不接外部 API |
| 影响 v0.1 已有功能 | ❌ 不影响 | 渐进增强,失败/不支持环境完全无感 |

### 0.2 渐进增强原则

- **检测优先**:UA 检测、`navigator.standalone`、`localStorage` 各自独立,任何一个失败时整个引导静默不出现
- **无 JS 退化**:JS 加载失败时,页面与 v0.1 完全等同,只是没有引导浮层 — 用户仍然可用
- **不阻塞首屏**:引导脚本 `defer` 加载,DOM 就绪后再决定是否注入浮层
- **可关闭**:用户 dismiss 后写 localStorage,有 TTL(微信 24h / iOS 7d),过期后再次提醒一次,**永远可以再 dismiss**
- **DOM 安全**:全部用 `createElement` / `createElementNS` + `setAttribute` / `textContent` 构造节点;**不用 `innerHTML`**(防 XSS + 工具链一致)

### 0.3 文件改动清单(完整)

| 状态 | 路径 | 说明 |
|---|---|---|
| ✏️ 改 | `src/main/resources/templates/fragments/layout.html` | head 段加 5 条 meta + 1 link manifest + 1 link apple-touch-icon-180.png(替换原 SVG)+ 1 script defer |
| ➕ 新 | `src/main/resources/static/manifest.webmanifest` | PWA 元数据 |
| ➕ 新 | `src/main/resources/static/js/mobile-guide.js` | 微信检测 + iOS PWA banner 逻辑(~150 行) |
| ➕ 新 | `src/main/resources/static/img/apple-touch-icon.svg` | 设计源 SVG(转 PNG 的输入,提交 git) |
| ➕ 新 | `src/main/resources/static/img/apple-touch-icon-180.png` | iOS 主屏图标 180×180(从 SVG 一次性生成,提交 git) |
| ➕ 新 | `src/main/resources/static/img/icon-192.png` | PWA 通用图标 192×192 |
| ➕ 新 | `src/main/resources/static/img/icon-512.png` | PWA 通用图标 512×512(同时用于 maskable) |
| ✏️ 改 | `src/main/java/com/family/finance/auth/SecurityConfig.java` | matchers 加 `/manifest.webmanifest` + `/js/**` |
| ✏️ 改(可能) | `src/main/java/com/family/finance/config/WebMvcConfig.java` | 仅当 `.webmanifest` MIME 不对时加 ContentNegotiation 配置 |
| ➕ 新 | `deploy/gen-icons.sh` | 一次性 SVG → PNG 生成脚本(改 SVG 时跑一次) |

---

## 1. FR-33 · 微信内浏览器引导

### 1.1 触发逻辑

```javascript
function shouldShowWeChatGuide() {
  if (!/MicroMessenger/i.test(navigator.userAgent)) return false;
  const dismissedAt = +(localStorage.getItem('wx_dismissed_at') || 0);
  const TTL_MS = 24 * 60 * 60 * 1000; // 24h
  return Date.now() - dismissedAt > TTL_MS;
}
```

**关键决策**:

- **不做白名单页面**:微信内打开任何页面都触发(包括 `/login`)。理由:登录前用户也应该被引导走,否则填密码后还会遇到 cookie 不持久问题。
- **不区分 iOS 微信 / Android 微信**:UA 都含 `MicroMessenger`,行为相同。
- **不区分 X5 / WKWebView**:无意义 — 微信内的限制对两个内核都成立。
- **dismiss = 写时间戳,非布尔**:这样 24h 后能自动恢复提示,无需额外定时任务。

### 1.2 DOM 结构(运行时注入,不预渲染)

引导浮层不预渲染在 HTML 里(避免对其它环境产生 DOM 噪音),完全由 JS 在运行时通过 `document.createElement` 注入。

注入后的 DOM 结构(节点级):

```
<div id="wx-guide-mask" role="dialog" aria-modal="true">       ← 全屏遮罩
  <svg viewBox="0 0 60 76" class="wx-guide-arrow">              ← 指向右上 ⋯ 的金色箭头
    <path d="..." />                                            ← 用 createElementNS 构造
    <path d="..." />
  </svg>
  <div class="wx-guide-card">                                   ← 引导卡
    <h3>📦 在浏览器中打开</h3>
    <p>微信内浏览器对账房功能支持不全...</p>
    <ol>
      <li><span>1</span> 点击右上角的 ⋯ 按钮</li>
      <li><span>2</span> 选择「在浏览器中打开」</li>
    </ol>
    <button>我知道了 · 继续在微信用</button>
    <div>24 小时内不再提示</div>
  </div>
</div>
```

样式使用内联(`element.style.cssText = ...`),避开污染全局 CSS、避开 Tailwind 动态构建的不确定性。动画 keyframes 一次性 `<style>` 注入。

### 1.3 行为细节

| 事件 | 行为 |
|---|---|
| 点击 dismiss 按钮 | 写 `localStorage.setItem('wx_dismissed_at', Date.now())`,移除 mask DOM |
| 点击遮罩本身(非引导卡) | 不响应(避免误触关闭) |
| 按 ESC | 不响应(微信无键盘场景,且家庭用户不依赖) |
| 横竖屏切换 | CSS 用 inset/百分比,自适应 |
| 页面切换 | localStorage 跨会话生效 |

### 1.4 文案(精确,与预览一致)

```
标题:    📦 在浏览器中打开
正文:    微信内浏览器对账房功能支持不全,建议改在 Safari 或 Chrome 中打开,
         可获得完整体验(添加桌面图标、登录态稳定、CSV 直接下载)。
步骤 1:  点击右上角 ⋯ 按钮
步骤 2:  选择「在浏览器中打开」
按钮:    我知道了 · 继续在微信用
角标:    24 小时内不再提示
```

文案与预览图 `preview/v0.2/pages/wechat-guide.html` 完全一致 — 不要在实施时改动文案,UI 与文案一并 review 通过。

---

## 2. FR-34 · iOS Safari PWA 添加到主屏

### 2.1 触发逻辑

```javascript
function shouldShowIosPwaBanner() {
  const ua = navigator.userAgent;
  const isIOS = /iPad|iPhone|iPod/.test(ua);
  const isSafari = /^((?!chrome|android|crios|fxios).)*safari/i.test(ua);
  const isWeChat = /MicroMessenger/i.test(ua);
  const inStandalone = window.navigator.standalone === true;
  const dismissedAt = +(localStorage.getItem('pwa_dismissed_at') || 0);
  const TTL_MS = 7 * 24 * 60 * 60 * 1000; // 7d

  return isIOS
      && isSafari
      && !isWeChat
      && !inStandalone
      && (Date.now() - dismissedAt > TTL_MS);
}
```

**关键决策**:

- **必须排除微信内 iOS Safari**:微信内 UA 在 iOS 上同时匹配 Safari 和 MicroMessenger。微信检测优先 — 实际代码里两条分支互斥(微信里只弹微信引导)。
- **`navigator.standalone === true`**:用户已经把账房加到主屏并从主屏点开 — 在 standalone 模式下不再骚扰。
- **延迟 1.5 秒弹出**:`setTimeout(showBanner, 1500)`。理由:首屏加载时间不打扰用户。
- **dismiss TTL 7 天**:比微信引导 24h 长很多。理由:iOS PWA 引导是"建议",不是必要;7 天内被提示一次足够。

### 2.2 DOM 结构(底部固定卡片)

```
<div id="pwa-banner" role="region" aria-label="添加到主屏">
  <button class="pwa-close" aria-label="关闭">✕</button>
  <div class="pwa-head">
    <div class="pwa-icon">账</div>      ← CSS 渲染棕青配色,与 brand 一致
    <div class="pwa-text">
      <h3>添加到主屏 · 像 App 一样使用</h3>
      <p>FAMILY · COUNTING · HOUSE</p>
    </div>
  </div>
  <ol>
    <li><span>1</span>点击底部分享按钮 ⤴</li>
    <li><span>2</span>滚动找到「添加到主屏幕」</li>
    <li><span>3</span>点击右上角「添加」</li>
  </ol>
  <div class="pwa-actions">
    <button>知道了</button>
    <button>7 天内不再提示</button>
  </div>
</div>
```

底部固定 + 滑入动画(transform: translateY(110%) → 0,400ms ease-out)。

### 2.3 layout.html head 改动

在 `templates/fragments/layout.html` 当前 head 段(已有 favicon / stylesheet / vendor scripts)**追加 + 替换** 1 处:

```diff
 <link rel="icon" type="image/svg+xml" ...> ← 保持不变
-<link rel="apple-touch-icon" th:href="@{/img/default-logo.svg(v=${buildVersion})}">
+<link rel="apple-touch-icon" sizes="180x180"
+      th:href="@{/img/apple-touch-icon-180.png(v=${buildVersion})}">
+<!-- v0.2 FR-34 · PWA 添加到主屏 -->
+<link rel="manifest" th:href="@{/manifest.webmanifest(v=${buildVersion})}">
+<meta name="theme-color" content="#1a1714">
+<meta name="apple-mobile-web-app-capable" content="yes">
+<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
+<meta name="apple-mobile-web-app-title" content="账房">
```

> ⚠️ **iOS Safari 不支持 SVG 作 apple-touch-icon**。当前 v0.1 的 layout 写的是 SVG 路径,iOS 加到主屏时会显示空白图标或 fallback 到首屏截图。这是 v0.1 遗留 bug,顺便修。

在 head 末尾(所有 link/meta 之后,vendor scripts 之后)加:

```html
<script th:src="@{/js/mobile-guide.js(v=${buildVersion})}" defer></script>
```

`defer` 确保不阻塞首屏渲染。

### 2.4 manifest.webmanifest 内容

新建 `src/main/resources/static/manifest.webmanifest`(注意:**扩展名是 `.webmanifest` 不是 `.json`**;MIME 类型 `application/manifest+json`):

```json
{
  "name": "家庭账房",
  "short_name": "账房",
  "description": "家庭资产 · 月度账册 · 仅限家庭内部使用",
  "lang": "zh-CN",
  "start_url": "/dashboard",
  "scope": "/",
  "display": "standalone",
  "orientation": "portrait",
  "theme_color": "#1a1714",
  "background_color": "#f6f1ea",
  "icons": [
    { "src": "/img/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/img/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
    { "src": "/img/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

**MIME 处理**:Spring Boot 3.x 默认能识别 `.webmanifest` 扩展名映射到 `application/manifest+json`(自 Spring 5.3+ 起内置)。

**先跑一次**,curl `-I` 看 Content-Type:

```bash
curl -I http://localhost:20000/manifest.webmanifest | grep -i content-type
```

- 如果回 `application/manifest+json` → 什么都不用改
- 如果回 `application/octet-stream` 或别的 → 加 § 4.2 的 `WebMvcConfig` 配置

### 2.5 SecurityConfig 改动

`src/main/java/com/family/finance/auth/SecurityConfig.java` 第 93 行(精确 patch):

```diff
-.requestMatchers("/login", "/login/error", "/static/**", "/css/**",
-    "/vendor/**", "/img/**", "/uploads/**", "/favicon.ico", "/error",
-    "/health", "/actuator/health").permitAll()
+.requestMatchers("/login", "/login/error", "/static/**", "/css/**",
+    "/vendor/**", "/img/**", "/uploads/**", "/js/**",
+    "/manifest.webmanifest", "/favicon.ico", "/error",
+    "/health", "/actuator/health").permitAll()
```

新增放行两项:
- `/manifest.webmanifest` — 浏览器拉 manifest 必须未登录可达(否则 302 → /login,manifest 解析失败)
- `/js/**` — 现有 SecurityConfig 漏掉(v0.1 没用到,v0.2 新增 `mobile-guide.js`)

---

## 3. 共享基建:`mobile-guide.js`

完整脚本结构(~150 行),放在 `src/main/resources/static/js/mobile-guide.js`。

### 3.1 模块结构

```
(IIFE)
  ├── 'use strict'
  ├── Storage 工具(localStorage 不可用降级 no-op)
  ├── UA 检测变量(isWX / isIOS / isSafari / inStandalone)
  ├── injectStyle(id, css)        — 一次性注入 keyframes
  ├── el(tag, css, text?)          — 创建 div/span/button 等普通节点的 helper
  ├── svgEl(tag, attrs)            — 创建 SVG 节点(createElementNS)的 helper
  ├── showWxGuide()                — FR-33 微信引导
  ├── showIosPwaBanner()           — FR-34 iOS PWA banner
  └── DOMContentLoaded 分发
```

### 3.2 关键设计点

| 点 | 决策 | 理由 |
|---|---|---|
| IIFE 包装 | `(function(){…})()` | 避免污染全局 |
| `'use strict'` | 用 | 防变量泄漏到 window |
| localStorage 降级 | try-catch 包装 | iOS 无痕模式 / 隐私沙箱可能抛 |
| **`createElement` 而非 `innerHTML`** | 全部 createElement | 防注入,工具链规则一致(claude-code 安全 hook 拦截 innerHTML) |
| SVG path 用 `createElementNS` + `setAttribute` | 是 | 同上,namespace 必须正确才能渲染 |
| 动画用 inline `<style>` 注入 | 是 | 避开依赖 Tailwind/外部 CSS;动画一次注入复用 |
| 微信 / iOS 互斥 | 微信优先 | 微信内不弹 iOS PWA banner(没意义,微信内不能加主屏) |
| Z-index | 微信 99999 / PWA banner 99998 | 高于一切应用层级,避免被遮挡 |

### 3.3 完整代码

```javascript
/* 家庭账房 v0.2 · 移动端引导(FR-33 + FR-34)
 * 渐进增强 — 失败/不支持环境完全静默。
 */
(function () {
  'use strict';

  // ---------- Storage 降级 ----------
  const S = (function () {
    try {
      localStorage.setItem('__t__', '1');
      localStorage.removeItem('__t__');
      return localStorage;
    } catch (e) {
      return { getItem: function () { return null; },
               setItem: function () {}, removeItem: function () {} };
    }
  })();

  // ---------- UA 检测 ----------
  const ua = navigator.userAgent;
  const isWX = /MicroMessenger/i.test(ua);
  const isIOS = /iPad|iPhone|iPod/.test(ua);
  const isSafari = /^((?!chrome|android|crios|fxios).)*safari/i.test(ua);
  const inStandalone = window.navigator.standalone === true;

  // ---------- DOM helpers(全部用安全 API) ----------
  function injectStyle(id, css) {
    if (document.getElementById(id)) return;
    const s = document.createElement('style');
    s.id = id;
    s.textContent = css;
    document.head.appendChild(s);
  }
  function el(tag, css, text) {
    const e = document.createElement(tag);
    if (css) e.style.cssText = css;
    if (text != null) e.textContent = text;
    return e;
  }
  function svgEl(tag, attrs) {
    const e = document.createElementNS('http://www.w3.org/2000/svg', tag);
    if (attrs) Object.keys(attrs).forEach(function (k) { e.setAttribute(k, attrs[k]); });
    return e;
  }

  // ============ FR-33 微信引导 ============
  function showWxGuide() {
    if (Date.now() - (+S.getItem('wx_dismissed_at') || 0) < 86400000) return;

    injectStyle('wx-guide-keyframes',
      '@keyframes wxBounce{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}');

    const mask = el('div', 'position:fixed;inset:0;background:rgba(20,16,12,.78);' +
      'backdrop-filter:blur(2px);z-index:99999;' +
      'font-family:-apple-system,BlinkMacSystemFont,"PingFang SC",sans-serif;');
    mask.id = 'wx-guide-mask';
    mask.setAttribute('role', 'dialog');
    mask.setAttribute('aria-modal', 'true');

    // 大箭头 SVG(指向右上 ⋯)
    const svg = svgEl('svg', { viewBox: '0 0 60 76' });
    svg.style.cssText = 'position:absolute;top:8px;right:24px;width:60px;height:76px;' +
      'animation:wxBounce 1.4s ease-in-out infinite;';
    svg.appendChild(svgEl('path', {
      d: 'M30 70 Q 34 35, 50 8',
      stroke: '#d6a04a', 'stroke-width': '3',
      'stroke-linecap': 'round', fill: 'none', 'stroke-dasharray': '5 4'
    }));
    svg.appendChild(svgEl('path', {
      d: 'M50 8 L 42 14 M50 8 L 56 16',
      stroke: '#d6a04a', 'stroke-width': '3', 'stroke-linecap': 'round'
    }));
    mask.appendChild(svg);

    // 引导卡
    const card = el('div', 'position:absolute;top:30%;left:24px;right:24px;' +
      'background:#fdf8ef;border:1px solid #1a1714;' +
      'box-shadow:0 14px 40px -8px rgba(0,0,0,.5);padding:22px;');

    card.appendChild(el('h3',
      'font:500 18px/1.2 "Noto Serif SC",serif;margin:0 0 10px;color:#1a1714;',
      '📦 在浏览器中打开'));

    card.appendChild(el('p',
      'font-size:13px;color:#3a3530;line-height:1.55;margin:0 0 14px;',
      '微信内浏览器对账房功能支持不全,建议改在 Safari 或 Chrome 中打开,可获得完整体验。'));

    const ol = el('ol', 'margin:0 0 14px;padding:0;list-style:none;display:flex;flex-direction:column;gap:8px;');
    [['1', '点击右上角 ⋯ 按钮'], ['2', '选择「在浏览器中打开」']].forEach(function (step) {
      const li = el('li',
        'display:flex;align-items:baseline;gap:10px;font-size:13px;color:#3a3530;');
      const num = el('span',
        'width:22px;height:22px;background:#1a1714;color:#fdf8ef;border-radius:50%;' +
        'display:inline-flex;align-items:center;justify-content:center;' +
        'font:500 11px monospace;flex-shrink:0;',
        step[0]);
      li.appendChild(num);
      li.appendChild(document.createTextNode(step[1]));
      ol.appendChild(li);
    });
    card.appendChild(ol);

    const btn = el('button',
      'width:100%;padding:9px;border:1px solid #1a1714;background:transparent;' +
      'font:11px/1 monospace;letter-spacing:.1em;color:#1a1714;cursor:pointer;',
      '我知道了 · 继续在微信用');
    btn.addEventListener('click', function () {
      S.setItem('wx_dismissed_at', String(Date.now()));
      mask.remove();
    });
    card.appendChild(btn);

    card.appendChild(el('div',
      'text-align:center;font:9px monospace;color:#888;margin-top:8px;letter-spacing:.18em;',
      '24 小时内不再提示'));

    mask.appendChild(card);
    document.body.appendChild(mask);
  }

  // ============ FR-34 iOS PWA Banner ============
  function showIosPwaBanner() {
    if (Date.now() - (+S.getItem('pwa_dismissed_at') || 0) < 7 * 86400000) return;

    injectStyle('pwa-banner-keyframes',
      '@keyframes pwaSlideUp{from{transform:translateY(110%);opacity:0}to{transform:translateY(0);opacity:1}}');

    const banner = el('div', 'position:fixed;left:10px;right:10px;bottom:90px;z-index:99998;' +
      'background:#fdf8ef;border:1px solid #1a1714;' +
      'box-shadow:0 -8px 28px -4px rgba(0,0,0,.18);padding:14px;' +
      'animation:pwaSlideUp .4s ease-out;' +
      'font-family:"Source Serif 4","Noto Serif SC",serif;');
    banner.id = 'pwa-banner';
    banner.setAttribute('role', 'region');
    banner.setAttribute('aria-label', '添加到主屏');

    // 关闭 ✕
    const x = el('button',
      'position:absolute;top:6px;right:8px;width:22px;height:22px;border:0;' +
      'background:transparent;color:#888;font-size:14px;cursor:pointer;',
      '✕');
    x.setAttribute('aria-label', '关闭');
    x.addEventListener('click', function () { banner.remove(); });
    banner.appendChild(x);

    // 头(图标 + 标题)
    const head = el('div', 'display:flex;gap:12px;align-items:flex-start;margin-bottom:10px;');
    head.appendChild(el('div',
      'width:44px;height:44px;border-radius:11px;flex-shrink:0;' +
      'background:linear-gradient(135deg,#c0915a,#7a5a39);' +
      'display:flex;align-items:center;justify-content:center;color:#fdf8ef;' +
      'font:500 22px "Fraunces",serif;box-shadow:0 2px 8px -1px rgba(122,90,57,.45);',
      '账'));
    const txtwrap = el('div', 'flex:1;padding-top:2px;');
    txtwrap.appendChild(el('h3',
      'font:500 15px/1.2 "Fraunces","Noto Serif SC",serif;color:#1a1714;margin:0;',
      '添加到主屏 · 像 App 一样使用'));
    txtwrap.appendChild(el('p',
      'font:9px/1 monospace;letter-spacing:.18em;color:#888;margin:3px 0 0;text-transform:uppercase;',
      'FAMILY · COUNTING · HOUSE'));
    head.appendChild(txtwrap);
    banner.appendChild(head);

    // 步骤
    const steps = el('ol', 'margin:0;padding:0;list-style:none;');
    [['1', '点击底部分享按钮 ⤴'],
     ['2', '滚动找到「添加到主屏幕」'],
     ['3', '点击右上角「添加」']].forEach(function (step) {
      const li = el('li',
        'display:flex;align-items:baseline;gap:6px;font:12px/1.4 inherit;color:#3a3530;padding:2px 0;');
      li.appendChild(el('span',
        'width:18px;height:18px;background:#1a1714;color:#fdf8ef;border-radius:50%;' +
        'display:inline-flex;align-items:center;justify-content:center;' +
        'font:10px monospace;flex-shrink:0;',
        step[0]));
      li.appendChild(document.createTextNode(step[1]));
      steps.appendChild(li);
    });
    banner.appendChild(steps);

    // 操作行
    const acts = el('div',
      'display:flex;gap:6px;margin-top:9px;padding-top:9px;border-top:.5px solid rgba(0,0,0,.1);');
    const ok = el('button',
      'flex:1;padding:7px;border:1px solid #c8c2b6;background:transparent;' +
      'font:10px monospace;letter-spacing:.1em;cursor:pointer;color:#1a1714;',
      '知道了');
    ok.addEventListener('click', function () { banner.remove(); });
    const skip = el('button',
      'flex:1;padding:7px;border:0;background:transparent;' +
      'font:10px monospace;letter-spacing:.1em;cursor:pointer;color:#888;',
      '7 天内不再提示');
    skip.addEventListener('click', function () {
      S.setItem('pwa_dismissed_at', String(Date.now()));
      banner.remove();
    });
    acts.appendChild(ok);
    acts.appendChild(skip);
    banner.appendChild(acts);

    document.body.appendChild(banner);
  }

  // ============ 触发分发(微信优先互斥) ============
  document.addEventListener('DOMContentLoaded', function () {
    if (isWX) {
      showWxGuide();
    } else if (isIOS && isSafari && !inStandalone) {
      setTimeout(showIosPwaBanner, 1500);
    }
    // 其它环境:静默
  });
})();
```

---

## 4. 静态资源生成

### 4.1 PWA / iOS 主屏图标(★ 用网站自己的 logo)

> **用户要求**:iOS 主屏图标必须是「账房」自己的设计,不能是默认 favicon、不能是浏览器自动截图。

#### 4.1.1 设计源(新文件 `static/img/apple-touch-icon.svg`)

现有的 `default-logo.svg` 是 64×64 浅色背景版,**作为 iOS 主屏图标不合适** — iOS 主屏背景是用户壁纸(深色居多),浅色 logo 显得苍白,硬币 + 屋顶细节在 60×60 缩略图下也看不清。

为 iOS 主屏 + PWA Android 主屏单独设计 `apple-touch-icon.svg`(基于 default-logo 改色块,放大主体),提交到 git:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- 实心墨色背景(iOS 自动加圆角,源 PNG 必须方形不带圆角不带 alpha) -->
  <rect width="512" height="512" fill="#1a1714"/>
  <!-- 屋顶(双坡,金棕色,放大) -->
  <path d="M 100 240 L 256 110 L 412 240" fill="none"
        stroke="#d6a04a" stroke-width="22"
        stroke-linejoin="round" stroke-linecap="round"/>
  <!-- 屋脊小烟囱 -->
  <path d="M 330 158 L 330 116 L 360 116 L 360 188"
        fill="none" stroke="#d6a04a" stroke-width="11"/>
  <!-- 硬币(黄铜色) -->
  <circle cx="256" cy="340" r="105" fill="#c4943e"/>
  <circle cx="256" cy="340" r="78" fill="none"
          stroke="#1a1714" stroke-width="2" opacity=".4"/>
  <!-- ¥ 符号 -->
  <g stroke="#1a1714" stroke-width="14" stroke-linecap="round"
     stroke-linejoin="round" fill="none">
    <path d="M 218 296 L 256 348 L 294 296"/>
    <line x1="256" y1="348" x2="256" y2="396"/>
    <line x1="220" y1="356" x2="292" y2="356"/>
    <line x1="220" y1="376" x2="292" y2="376"/>
  </g>
  <!-- 朱红印泥点(右上,品牌一致) -->
  <circle cx="448" cy="64" r="22" fill="#9f4a3a"/>
</svg>
```

**重要**:
- 源 SVG 是 **方形 + 实心墨底**(`#1a1714`),iOS 自动加圆角和阴影
- 主体元素改成金棕色(`#d6a04a`)+ 黄铜硬币色(`#c4943e`),墨色背景上对比鲜明
- 朱红印泥点保留(品牌一致性)
- ViewBox 512 与 512 PNG 一致,锐利不糊

#### 4.1.2 PNG 生成(三个尺寸)

服务器需安装 `librsvg2-bin`(轻量 ~2MB,Ubuntu/Debian):

```bash
sudo apt install -y librsvg2-bin
```

跑一次性脚本生成 3 张 PNG。新建 `deploy/gen-icons.sh`:

```bash
#!/usr/bin/env bash
# 一次性生成 PWA / iOS 主屏图标 PNG(改了 SVG 后重新跑)
# 用法: bash deploy/gen-icons.sh
set -e

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
SRC="$ROOT/src/main/resources/static/img/apple-touch-icon.svg"
DIR="$ROOT/src/main/resources/static/img"

if ! command -v rsvg-convert &> /dev/null; then
  echo "✗ rsvg-convert not found. Install:  sudo apt install -y librsvg2-bin"
  exit 1
fi

if [[ ! -f "$SRC" ]]; then
  echo "✗ Source not found: $SRC"
  exit 1
fi

rsvg-convert -w 180 -h 180 "$SRC" -o "$DIR/apple-touch-icon-180.png"
rsvg-convert -w 192 -h 192 "$SRC" -o "$DIR/icon-192.png"
rsvg-convert -w 512 -h 512 "$SRC" -o "$DIR/icon-512.png"

echo "✓ 3 PNGs generated. Now: git add + commit + redeploy."
ls -la "$DIR"/{apple-touch-icon-180,icon-192,icon-512}.png
```

**生成的 PNG 提交到 git**(避免每次 deploy 都重跑;只有改 SVG 时才重跑):

```
src/main/resources/static/img/apple-touch-icon.svg       ← 设计源(改这里)
src/main/resources/static/img/apple-touch-icon-180.png   ← iOS 主屏专用(<link rel="apple-touch-icon">)
src/main/resources/static/img/icon-192.png               ← manifest 引用
src/main/resources/static/img/icon-512.png               ← manifest 引用 + maskable
```

> **备选方案**(如果服务器无法装 librsvg2-bin):本地 macOS 用 `qlmanage -t -s 180 apple-touch-icon.svg`,或 Figma / Sketch 手工导出,再 scp 上去。

### 4.2 .webmanifest MIME type(可能需要)

Spring Boot 3.x 默认能识别 `.webmanifest` 扩展名映射到 `application/manifest+json`(自 Spring 5.3+ 起内置)。

**先跑一次** `curl -I http://localhost:20000/manifest.webmanifest`:

- 回 `application/manifest+json` → 不用改
- 回 `application/octet-stream` 或别的 → 在 `WebMvcConfig.java` 加:

```java
@Override
public void configureContentNegotiation(ContentNegotiationConfigurer cfg) {
    cfg.mediaType("webmanifest", MediaType.parseMediaType("application/manifest+json"));
}
```

---

## 5. 渐进部署 / 回退策略

### 5.1 部署顺序(按风险从低到高)

1. **静态资源先行**:把 `manifest.webmanifest`、`apple-touch-icon.svg`、3 个 PNG 加进 `static/`,提交后部署 — 这一步对所有用户透明,不出现任何 UI 变化。curl 验证 6 个文件 200。
2. **SecurityConfig 放行 + WebMvcConfig 改 MIME**(如需):重启后 curl 验证 `/manifest.webmanifest` 200 + 正确 Content-Type。
3. **layout.html head 改动**:加 5 个 meta + 1 个 link manifest + 1 个 link apple-touch-icon-180.png + 1 个 script defer — 上线后:
   - 桌面浏览器 / Chrome / Firefox / Edge:无任何视觉变化
   - iOS Safari:DevTools 显示"Web App Manifest 检测成功",但 UI 无变化(banner 还没注入,`mobile-guide.js` 还没创建)
4. **mobile-guide.js 上线**:这一步开始有 UI 变化 — 单独 commit,方便快速 revert。

### 5.2 紧急关闭(三层)

| 层级 | 操作 | 影响 | 生效 |
|---|---|---|---|
| **L1 紧急** | 把 `mobile-guide.js` 内容替换成单行 `(function(){})()` | 所有用户立即不再看到引导 | 部署 + buildVersion 自动失效缓存 |
| **L2** | layout.html 注释掉 `<script ..mobile-guide.js>` | 同上,更彻底 | 同上 |
| **L3 完全回退** | revert SecurityConfig + WebMvcConfig + layout.html + 删 manifest + 删 PNG | 完全回到 v0.1 状态 | 全量部署 |

---

## 6. 验收测试

### 6.1 自动化(curl + grep,加进 `/tmp/qa-run.sh`)

```bash
# ---------- v0.2 FR-34 PWA ----------
section "v0.2 · FR-34 · PWA manifest + iOS 配套"

# 1. manifest 200 + 正确 MIME
ct=$($CURL -o /dev/null -w "%{content_type}" "$BASE/manifest.webmanifest")
[[ "$ct" == *"manifest+json"* ]] && log_ok "FR34-1 manifest Content-Type=application/manifest+json" \
  || log_bad "FR34-1 manifest MIME 错" "got=$ct"

# 2. manifest 内容含必要字段
$CURL "$BASE/manifest.webmanifest" -o /tmp/qa-manifest.json
{ grep -q '"name"' /tmp/qa-manifest.json && grep -q '"start_url": "/dashboard"' /tmp/qa-manifest.json \
  && grep -q '"display": "standalone"' /tmp/qa-manifest.json && grep -q '"icons"' /tmp/qa-manifest.json; } \
  && log_ok "FR34-2 manifest 字段齐(name/start_url/display/icons)" \
  || log_bad "FR34-2 manifest 字段缺" "see /tmp/qa-manifest.json"

# 3. 三张 PNG 都 200
for f in apple-touch-icon-180.png icon-192.png icon-512.png; do
  code=$($CURL -o /dev/null -w "%{http_code}" "$BASE/img/$f")
  [[ "$code" == "200" ]] && log_ok "FR34-3 /img/$f = 200" \
    || log_bad "FR34-3 /img/$f" "code=$code"
done

# 4. layout.html 含 4 个 apple meta + manifest link + apple-touch-icon-180.png
$CURL -b $COOKIE "$BASE/dashboard" -o "$TMP" -w ""
{ grep -q 'name="apple-mobile-web-app-capable"' "$TMP" \
  && grep -q 'name="apple-mobile-web-app-status-bar-style"' "$TMP" \
  && grep -q 'name="apple-mobile-web-app-title"' "$TMP" \
  && grep -q 'rel="manifest"' "$TMP" \
  && grep -q 'apple-touch-icon-180.png' "$TMP"; } \
  && log_ok "FR34-4 dashboard head 含 PWA meta + apple-touch-icon-180" \
  || log_bad "FR34-4 PWA meta 缺" "missing"

# 5. mobile-guide.js 200(未登录可达,SecurityConfig 放行 /js/**)
code=$($CURL -o /dev/null -w "%{http_code}" "$BASE/js/mobile-guide.js")
[[ "$code" == "200" ]] && log_ok "FR34-5 /js/mobile-guide.js = 200" \
  || log_bad "FR34-5 mobile-guide.js" "code=$code"

# 6. 未登录可达 manifest
code=$($CURL -o /dev/null -w "%{http_code}" "$BASE/manifest.webmanifest")
[[ "$code" == "200" ]] && log_ok "FR34-6 未登录可达 manifest" \
  || log_bad "FR34-6 manifest 被拦" "code=$code"

# ---------- v0.2 FR-33 微信引导(只能验脚本被引用) ----------
$CURL -b $COOKIE "$BASE/dashboard" -o "$TMP" -w ""
grep -q 'mobile-guide.js' "$TMP" && log_ok "FR33-1 layout 引用 mobile-guide.js" \
  || log_bad "FR33-1 mobile-guide.js 未引入" "missing"
```

### 6.2 真机手测(必须做的 8 条)

| ID | 设备 | 步骤 | 预期 |
|---|---|---|---|
| **FR33-M1** | iOS 微信 | 微信里点账房链接 | 看到全屏遮罩 + 引导卡 + 大箭头指右上 ⋯ |
| **FR33-M2** | iOS 微信 | dismiss 后立即重新进入 | 不再弹遮罩 |
| **FR33-M3** | iOS 微信 | dismiss 后等 25 小时再进入 | 再次弹遮罩 |
| **FR33-M4** | Android 微信 | 同 M1 | 行为相同 |
| **FR34-M1** | iPhone Safari | 直接打开 https://...account / dashboard | 1.5 秒后底部弹卡片 |
| **FR34-M2** | iPhone Safari | 按引导:分享 → 添加到主屏 → 添加 | **主屏出现金棕"账"图标(墨底硬币 + 屋顶,非默认 favicon)**,点击进入是 standalone(无 Safari UI) |
| **FR34-M3** | iPhone (主屏入口) | 重新打开账房(从主屏图标) | banner 不再弹(`navigator.standalone === true`) |
| **NEG-1** | macOS Chrome / Firefox / Edge | 打开账房 | banner 不弹、遮罩不弹 |
| **NEG-2** | iOS Chrome / Firefox(CriOS / FxiOS) | 打开账房 | 都不弹(虽然 iOS 但不是 Safari) |

### 6.3 浏览器开发者工具验证(每次部署)

- iOS Safari Inspector / 桌面 Chrome DevTools `Application` → `Manifest`:
  - 显示 "家庭账房" + 3 个 icons 预览(看到墨底金棕"账"硬币,不是空白)
  - "Identity" 段无警告
  - "Installability" 段全绿
- Lighthouse PWA 审计 ≥ 90 分

---

## 7. 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 微信不同版本 UA 字符串变化 | 低 | 引导误判/漏判 | 用宽松正则 `/MicroMessenger/i`,长期稳定;真出现 patch UA |
| iOS Safari UA 与第三方 in-app browser 重叠(微博/小红书内嵌) | 中 | banner 在不该弹的地方弹 | UA 检测里排除常见 in-app(crios/fxios + 已隐式排除微信);后续如出现 case 增量补 |
| `localStorage` 在隐私模式抛异常 | 低 | banner 每次都弹 | 已有 try-catch 降级为 no-op getItem |
| `.webmanifest` MIME 不对导致 Chrome 报警 | 中 | manifest 解析失败 | 部署后 curl `-I` 确认 Content-Type,失败时加 WebMvcConfig MIME 配置 |
| **iOS 主屏图标显示空白 / 默认 favicon** | **中** | **FR-34 体验崩 — 用户看到的不是品牌图标** | 严格用 PNG(非 SVG),提供 180×180 尺寸,且明确 `<link rel="apple-touch-icon" sizes="180x180">`;DevTools 验证 manifest icons 全绿 |
| `librsvg2-bin` 未装 | 中 | gen-icons.sh 跑不起来 | `gen-icons.sh` 开头检测 + 提示 install 命令;备选用本地导出 scp |

---

## 8. 工时估计

| 任务 | 估时 |
|---|---|
| 设计 `apple-touch-icon.svg`(基于 default-logo 改色 + 放大主体) | 30 min |
| 装 librsvg2-bin + 写 `deploy/gen-icons.sh` + 跑生成 3 PNG + git add | 15 min |
| 写 `manifest.webmanifest` + curl 验 MIME | 15 min |
| 写 `mobile-guide.js`(完整 ~150 行) | 60 min |
| 改 `layout.html` head 段(replace + 5 行 meta + 1 行 script) | 15 min |
| 改 `SecurityConfig.java` 1 行 | 5 min |
| 真机手测准备 + 跑 8 条用例(iOS Safari + iOS 微信 + Android 微信 + 桌面 Chrome) | 60 min |
| 写 QA 自动化(7 条 curl 用例)加进 `/tmp/qa-run.sh` | 20 min |
| 同步 `prd/v0.2.md` § 7 + `docs/qa-cases.md` 新段 | 20 min |
| **合计** | **~ 4 小时** |

> 上限按 **5.5 小时** 准备(MIME 需手工 + PNG 设计返工 + 真机环境调试)。

---

## 9. 与 v0.1 / v0.2 其它 FR 的关系

- **不破坏 v0.1**:所有 78 条 v0.1 QA 必须继续 PASS。新引入的 5 行 head meta + 1 个 defer script 不改变任何 v0.1 行为;唯一替换的 apple-touch-icon 从 SVG → PNG,实际上是修了 v0.1 的隐藏 bug。
- **预备 v0.2 后续 FR**:`mobile-guide.js` 留出扩展点,以后做 FR-30 账户详情页的"账户视角小贴士"也可以注入到这个文件里(共享 Storage 工具、共享 UA 工具)。
- **v1.0 PWA 推送**:本期不做(iOS 16+ 才支持,且需要 Service Worker)。`manifest.webmanifest` 已写好,加 SW 时不用改 manifest。

---

## 10. PRD / QA 同步清单

- [x] `prd/v0.2.md` § 7 验收看板:已勾选 FR-33 / FR-34 全部验收项
- [x] `docs/qa-cases.md` 新增"v0.2 · FR-33 / FR-34"段(8 条真机手测 + 7 条自动化)
- [x] **2026-05-10 推进**:FR-30/31/32/36/37/40/40a/40b/40c/40d 全部完成并验收
- [x] `tech-design/v0.2-checkup.md`(独立文档)记录决策 1-19,18 个架构决策选型 + 取舍

---

## 11. v0.2 完整交付(2026-05-10)

| 阶段 | FR | 已交付 | 单测 | QA |
|---|---|---|---|---|
| 阶段 1 | FR-36 + FR-37 + FR-40d | 顶部 nav · /accounts pills · 编辑/向导类目下拉 · /admin/product-categories · /checkup 占位 · V10/V11 migrations | — | 15 |
| 阶段 2 | FR-40b | /checkup?account=X 实页 · 4 类型分支卡 · MaxDrawdown / NavSeriesBuilder / BenchmarkComparator / AccountDiagnoseService | 26 | 8 |
| 阶段 3 | FR-40a + FR-40c | /checkup family 实页 · 16 条规则(10 账户 + 6 家庭)· LLM 文案润色层(Qwen + DeepSeek + Validator + 缓存 + 熔断)· HTMX polish endpoint | 27 | 10 |
| 阶段 4 | FR-30 + FR-31 + FR-32 | LedgerExporter · /accounts/{id}/ledger.csv · entry ledger ⋮ 删除按钮 · 软删 endpoint(反向冲销 + CLOSED 拒写) | — | 13 |
| 阶段 5(2026-05-10 当晚) | FR-38 + FR-40e + 决策 20 | dashboard KPI deep-link 5 张 · /checkup#锚点(allocation/risk/liquidity/return)· reports 加风险等级 doughnut + 风险敞口明细表 · **AI 综合诊断升级**(新 LlmDiagnoseService + AiDiagnoseController + GET /checkup/diagnose endpoint + 真名映射成员A/B/C + 反映射 + Validator 重写禁词扫描+真名扫描)· LLM 真实联调 PASS(qwen-plus 9s 内返回 500 字综合诊断) | 7 新 + 6 重写 | 7 |
| **合计** | | | **76 PASS** | **152 PASS / 0 FAIL / 1 SKIP** |

**v0.2 全部 FR 已完整闭环**(2026-05-10):
- 数据底座:V10 软删 + V11 类目体系 ✅
- 账本视角:FR-30 / FR-31 / FR-32 ✅
- 移动端引导:FR-33 / FR-34 ✅
- 资产体检:FR-36 / FR-37 / FR-38 / FR-40 / FR-40a / FR-40b / FR-40c(决策 20 AI 综合诊断)/ FR-40d / FR-40e ✅
- LLM 真实接入:Qwen-Plus 主用 + DeepSeek-Chat 备用,真实调用已验证 ✅
- 隐私边界:除主理人真名(映射成员A/B/C)外全部上传 ✅

---

## 附 · 修订 · 体检提醒的「不适用」(2026-09-24 · issue #22)

PRD FR-40a 的「✕ 这条不适用」从 v0.2 起就没实现:模板写的是 `onclick="advice.dismiss(ruleId, accountId)"`,这个函数从没写过,后端也没有接口。点击时浏览器报 `advice is not defined`,服务端看不到任何请求。

### 存在哪

| 方案 | 做法 | 取舍 |
|---|---|---|
| A · localStorage(原 PRD) | 前端按 `scope/account/rule` 存,30 天过期 | 只在一台设备的一个浏览器里生效。家里另一个人、换台手机都还看得到;清缓存就回来。家庭账房是全家共用的,这等于没点 |
| B · 新表 `advice_dismissal` | `family_id + rule_id + account_id + created_at` | 最规整,能记谁、何时点的。但要迁移;条目量极小(规则总共二十来条),为它加表不划算 |
| **C · 家庭级配置(选定)** | `family_runtime_config` 键 `checkup_advice_dismissed`,一行一条 `ruleId|accountId`(家庭级记 `*`) | 零迁移、全家一致、自带 `family_id` 隔离。`value_text` 512 字符,超出时丢最早标的(实际到不了) |

B 没选:当前没有「谁点的 / 什么时候点的」的需求;真要加,从 C 迁到 B 是一次性搬运,不影响页面。

### 过滤放在哪一层

`AdviceDismissService.visible(familyId, List<Advice>)` —— 规则引擎照常算全部命中,**展示前**过滤。三个出口都走它:家庭体检、账户体检(`CheckupController`)、AI 综合诊断(`AiDiagnoseController`,用户说了不适用,就不该再拿它去问 AI)。

没放进 `AdviceEngine` 里:引擎是纯函数(输入资产事实、输出命中),混进「这家人点过什么」会让它依赖配置、单测要造配置。

### 接口

- `POST /checkup/advice/dismiss`(`ruleId`,可选 `accountId`)· `POST /checkup/advice/restore`(可选 `accountId`,不带 = 恢复家庭级)
- 普通表单提交 + 302 回到 `/checkup#checkup-advice`(或 `?account=ID#checkup-advice`),**不依赖任何脚本** —— 这个按钮就是栽在脚本上的
- `accountId` 必须是本家庭的账户,否则整次请求忽略。不能当成「没传账户」存成家庭级:那会把一个针对账户的操作悄悄变成对全家生效(端上验收时抓到的,护栏 `v1246-ADVICE-DISMISS-FOREIGN-ACCOUNT`)

### 不设过期

原 PRD 的 30 天自动回来没有保留:「不适用」多是结构性判断,周期性冒出来只会让人觉得按钮坏了。页面上常驻「已隐藏 N 条标成「不适用」的提醒 · 全部恢复」作为撤回入口;全部标完时整块区域不能跟着隐藏,否则恢复入口也没了。

