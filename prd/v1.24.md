# PRD · v1.24 · 支出分析:从「摆出来给你看」到「所以呢」

> 状态:**待评审(PRD v1 + preview)**
> 目标版本:app.version = 1.24.0(写 PRD 时即 bump)
> preview:`preview/v1.24/expense-analysis.html`
> 来源:维护者 2026-09-20 原话 ——「上个版本增加了详细的支出明细,但针对这部分数据**价值获取分析没有做**」

---

## 0 · 这一版在解决什么

### 0.1 背景与现状

v1.21 + v1.22 把**进数**这条线做完了:自定义两层类目、支付宝/微信账单整月导入、
逐行校准、分类/账户逐笔可改、批次可撤。现在 `cash_flow` 里躺着一批**带分类的逐笔支出**。

但**读**这条线只做了三块,全在报表页 `_expense-cat-mix.html`(131 行):

| 现有组件 | 它回答什么 | 缺什么 |
|---|---|---|
| **本期构成** 堆叠条 + 类目列表 + 下钻 | 这个月钱花在哪 | 没有「和以前比怎么样」 |
| **年度累计** 横条 | 今年至今各类目多少 | 同上 |
| **近 12 期趋势** 柱 | 每月总支出的高低 | 只有总额,拆不开;**看得出变了,看不出为什么变** |

三块都是**陈列**:把数据整整齐齐摆出来,然后就没有然后了。用户看完知道「餐饮 ¥3,182 占 31%」,
但下一个问题 ——「**所以呢?**」—— 页面不回答。

**更要命的一点**:`月均支出` 在这个 app 里**不是一个展示字段,是一个承重结构**。

```
紧急储备月数 = 流动资产 ÷ 月均支出        FactViewServiceImpl.java:126-129
应急金目标   = 月均支出 × N 月            LiquiditySurplus.java:41
体检 · 流动性 = RuleContext(avgMonthlyExpense) CheckupController.java:80-96
报表 · 储蓄率 = (收入 − 支出) / 收入      ReportsController.java:591
```

而今天的 `月均支出` 是**近 12 个已关账期的一个扁平平均**(`FactViewServiceImpl.averageExpense`,
走 `expenseLedger.recentClosed(familyId, 12)`)。它把装修、买车、一次旅游、一笔大额医疗
**和每月的水电房贷混在同一个平均里**。后果是:

- 去年装修花了一笔 → 月均支出被永久抬高 12 个月 → **紧急储备被低估、FIRE 目标被高估**
- 而用户在页面上**看不到这件事**,也没有任何地方能说「那笔不算」

也就是说:我们已经在用支出数据做决策了,**只是用的是一个粗糙的版本**。
v1.21 把细节数据拿进来了,但决策层还在吃那个粗糙的平均数。

### 0.2 服务谁

**维护者本人 + 自建用户里已经在用 v1.21 导入的那部分。**

不是给「想省钱的人」——那是记账 App 的用户。是给**已经把钱记清楚了、想知道这对自己家的
资产和目标意味着什么**的人。非技术家人是**次要受益者**:她要能看懂「这个月为什么多花了」,
不需要看懂刚性弹性那一层。

### 0.3 主推动力

**让支出数据参与决策,而不是参与展示。**

一句话判据,用来砍 FR:**这个组件能不能改变用户的一个决定?**
不能的就是陈列,陈列已经够多了。

### 0.4 和竞品的根本分歧:我们不做预算

调研(§1)里三个主流中文记账 App —— 鲨鱼记账、钱迹、随手记 —— 支出分析的**重心全在预算**:
月度预算 + 分类预算 + 超支预警 + 日历红蓝标。

**我们不跟。** `AGENTS.md` §1「刻意不做」里写着**预算包络**,这一版不推翻它。理由不是
「做不了」,是**预算解决的是「别花超」,我们解决的是「这些钱花掉之后,家底和目标怎么样了」**。
前者是月度自律工具,后者是资产决策工具 —— 前者每天要看,后者每月看一次。
我们的硬约束是 10 分钟/月,只有后者装得进去。

### 0.5 本版新引入的名词

| 名词 | 定义 |
|---|---|
| **支出性质** | 每个类目的一个标记,三选一:**刚性** / **弹性** / **一次性**。与 `category_code`(性质:consumption/loan_payment/…)和 `expense_category_id`(钱花在哪)**是第三个正交维度**,不许合并(理由见 §6.3) |
| **一次性支出** | 偶发大额、不代表日常生活水平的开销(装修/买车/旅游/大额医疗/婚丧)。它**不该进月均支出**,因为月均支出的用途是回答「我每个月过日子要多少钱」 |
| **常态月均** | 剔除一次性支出后的月均。**并列**于既有的 `月均支出`,**不取代它** |
| **支出归因** | 本期与上期的差额**拆到类目**的瀑布。与报表页已有的「资产变化归因」是同一种表达,换了对象 |
| **周期性支出** | 类目 + 金额近似恒定 + 连续多期出现的笔,推定为订阅/月供/保费一类 |

既有名词(性质 / 消费分类 / 口径 A/B / `affects_balance` / 封板 / 已定稿)单一定义在
`AGENTS.md` 与 `prd/v1.21.md`,这里不重复。

---

## 1 · 调研:别人怎么做支出分析

> 维护者要求「至少 3 个热门记账软件,至少包含鲨鱼记账」。实际调研 5 个:
> 中文 3 个(鲨鱼记账 / 钱迹 / 随手记)+ 海外 2 个(Copilot Money / Monarch Money),
> 外加一篇把「报表该怎么设计」讲透的长文(少数派 · MoneyWiz 实操)。
> 海外那两个是**唯一和我们定位重叠**的样本 —— 它们同时管净资产和支出,中文三个基本只管支出。

### 1.1 · 鲨鱼记账(维护者点名)

**基本盘**:生活记账 App,暖黄色调,主打「极简操作」。iOS/Android/鸿蒙/Windows 全平台。

**怎么记**:最少 **2 步** —— 顶部切「支出/收入」页签(默认支出)→ 点类目 → 弹数字键盘输金额 → 完成。
备注会被系统记下来,变成**下次记账的快速标签**(一个很聪明的低成本习惯学习)。

**怎么分析 —— 页面结构**(这是我们要抄的部分):

```
┌─ 明细(首页) ────────────────────┐   ┌─ 图表页 ─────────────────────────┐
│ ┌──────────────────────────────┐ │   │ [ 周 ][ 月 ][ 年 ]   ← 分段切换   │
│ │ 2026-09  ▼                   │ │   │ 总支出 ¥12,600  日均 ¥420        │
│ │ 收入 ¥28,000   支出 ¥12,600  │ │   │                                  │
│ └──────────────────────────────┘ │   │  ╭─╮      ╭╮      折线:每天/每月 │
│  09-18 ─────────────────────     │   │ ╱  ╰─╮  ╭─╯╰─╮    消费趋势        │
│   餐饮 · 外卖          −¥38      │   │╱      ╰─╯     ╲                   │
│   交通 · 地铁          −¥6       │   │                                  │
│  09-17 ─────────────────────     │   │ 支出排行(条形)                  │
│   购物 · 日用          −¥212     │   │ 餐饮美食 ████████████ ¥3,182 ▲红 │
│   ...                            │   │ 交通出行 ███████ ¥1,940          │
│  (按天分栏 · 单条可点开改)       │   │ 购物消费 █████ ¥1,430            │
└──────────────────────────────────┘   └──────────────────────────────────┘
```

**三个值得偷的细节**:

1. **周/月/年分段切换 + 同时给「总额」和「平均值」**。平均值这个数很关键 ——
   它让不同长度的周期可比。我们今天的 12 期趋势柱只有总额,没有基准线。
2. **占比上升的类目打红色标记**。这是整个中文记账圈里**唯一一个主动说「所以呢」的地方** ——
   不是让用户自己比,是系统指出来。极低成本,极高价值。
3. **「必要消费 / 冲动消费」用颜色区分**。方向对(承认支出不是均质的),
   但用的词是**价值判断**(「冲动」),我们不跟这一点 —— 见 §4。

**它的弱点**:饼图、预算、数据导出、搜索全在 VIP 后面;图表页**看得出高低,看不出为什么**
(体验报告的原话是「图表版块相对成熟」,但通篇没提归因)。

### 1.2 · 钱迹

**基本盘**:无广告、极简,3 步记一笔。3000 万下载 / 600 万用户,评分 4.9。

**怎么分析**:
- 统计支持**年 / 月 / 自定义时间段**,可对**单个分类**做详细统计
- 三种图:**饼图 / 条形图 / 折线图**
- **多维度标签**(商家、项目、城市)—— 一笔可以挂多个标签,统计按标签汇总
- **资产负债表** + **资产曲线**(少见:一个记账 App 做到了净资产视角)
- 预算:年度/月度总预算 + 分类预算,**日均预算动态更新**,接近/超限日历标红

**值得偷的**:
1. **「自定义时间段」**。月不是唯一周期 —— 少数派那篇说得更狠:「**最佳的统计周期是工资发放周期**」。
   我们的账期天然就是月,但 v1.23 刚做完「关账宽限」,账期边界已经是可配置的了。
2. **标签维度与分类维度正交**。我们的「支出性质」本质上是同一个思路的极简版:
   **不做自由标签(维护成本高),只做一个三值的正交维度**。

### 1.3 · 随手记

**基本盘**:中文记账里体量最大、最接近「家庭财务」定位的一个。三端同步(App/网页/PC)。

**怎么分析**:
- 内置 **5 种报表**(iPad 版号称近 20 种图表):开支走向 / 预算比较 / **资产负债** / **资金流动** / 收支比例
- **首页即报表** —— 当前版本把报表提到了第一屏,不再是二级页面
- **多账本 + 跨账本报表**(旅游账/装修账/宝宝账/结婚账…)
- **自定义月起始日**,设置后流水、报表、预算全按这个周期算
- AI 记账:语音「早餐15元,通勤打车28元」自动拆分入账;截图/小票识别

**值得偷的**:
1. **「情景账本」其实就是我们的「一次性支出」**。用户之所以要单开一个「装修账」,
   正是因为他知道**装修的钱不该和日常混在一起看**。随手记的解法是让用户手动开一个账本
   (重),我们的解法是给类目/笔打一个标记(轻)——**同一个洞察,成本差一个数量级**。
2. **首页即报表**。用户不是来看流水的,是来看结论的。

### 1.4 · Copilot Money(海外 · 支出分析最强)

**基本盘**:Apple 生态优先,$95/年。定位是 **tracker, not a financial concierge**。

**怎么分析**:
- **每用户一个机器学习分类模型**,你纠正它就学。首轮准确率约 91–93%,
  轻量纠正几个月后 >95%。**约 30 笔之后自动分类开始生效**
- **Recurring 专页**:从交易模式里**自动识别订阅和账单**,金额变化时更新并提醒 ——
  专门对付 "subscription creep"(悄悄涨价的订阅)
- **Intelligence 信息流**:自动生成的个性化洞察
- 自然语言搜索交易
- 明确**不做** zero-based budgeting;它是**回溯型**的:告诉你钱已经怎么花了

**值得偷的**:
1. **Recurring 识别是这次调研里投入产出比最高的一个功能**。中文三个 App **一个都没做**。
   它抓的是**无感扣款** —— 那是一类用户永远不会主动去查、但确实在漏的钱。
2. **「回溯型而不是规划型」正是我们的定位**。Copilot 是海外最贵的那一档,
   它主动放弃了预算 —— 这是一个独立证据,说明「不做预算」不是我们在自我安慰。

### 1.5 · Monarch Money(海外 · 家庭协作 + 净资产,和我们最像)

**基本盘**:$99.99/年,**household-first**(有独立的配偶登录 + 只读分享给理财顾问)。
把投资、退休、负债**折进一个可定制的净资产视图** —— 这是所有样本里离我们最近的一个。

**怎么分析**:
- **Weekly Recap**:每周一段摘要,串起**支出 / 储蓄 / 收入 / 净资产**的重大变化,
  突出总支出与**主要驱动因素**、现金流趋势、**订阅变化**、净资产周环比
- **Month in review**:月末报告 = **Top 消费类目** + 月度现金流趋势 + **净资产变化图(按资产/负债拆)**
- **Insights**:页面上点一下「✦」,就地给出当前视图的要点
- 预算有两套模型:**Flex**(固定 / 非月度周期性 / 弹性 —— 三分!)或 **分类限额**

**值得偷的 —— 这是整轮调研最重要的两条**:

1. **Monarch 的 Flex 预算把支出分成「固定 / 非月度周期性 / 弹性」**。
   这和我们 §0.5 的**刚性 / 一次性 / 弹性**三分是同一个抽象,**而且是各自独立想出来的**。
   一个 $100/年的美国产品和我们撞了同一个三分法,说明这个切法是对的。
   差别在用途:Monarch 用它**定预算**,我们用它**修正月均支出的分母**。
2. **「Month in review」= 支出 Top 类目 + 现金流 + 净资产变化,三件事放在一份报告里。**
   我们的报表页**已经有后两件了**(封板快照 / 资金流瀑布 / 环比同比 / 归因),
   缺的恰恰是第一件。也就是说 —— **我们不需要新建一个页面,我们需要把缺的那一块补进已有的报告。**

### 1.6 · 少数派 ·「如果不做报表分析,那你每天记账也可能只是白费功夫」(MoneyWiz 实操)

这篇把「报表该有哪几张」讲得最透,作者的框架是 **2 必修 + 2 选修**:

| | 报表 | 回答 | 图 |
|---|---|---|---|
| 必修 1 | **资产净值走势** | 有多少钱 | 折线(可按账户拆)+ 现金流柱图 |
| 必修 2 | **支出分类统计** | 钱花到哪去 | 柱图(一级可下钻二级)+ 分类走势折线 |
| 选修 1 | **品牌消费集中度** | 经常跟谁交易 | 按「交易对象」的饼图 + 单品牌走势 |
| 选修 2 | **预算报表** | 花超没有 | 预算 vs 实际的柱图(饼图在超支后会失去意义) |

三句作者原话,值得直接抄进我们的设计原则:

> - 「**没有统计数据的输入,哪来的数据分析输出?**」—— 这正是 v1.21 做完之后 v1.24 才有可能存在的理由。
> - 「**最佳的统计周期是工资发放周期**」,先扣掉房租、话费、订阅费等**固定支出**,再看剩余的变量。
> - 报表的目的是「**调整**」而非「**改正**」——「**花钱永远没有对错**」,关键是找到平衡点。

第三句直接决定了我们的文案纪律(§4):**不做价值判断**。鲨鱼记账的「冲动消费」标签、
一堆 App 的「你这个月超支了!」,都踩在这条线上。

### 1.7 · 通用分析框架(多方交叉后收敛出来的)

| 维度 | 指标 | 用途 | 我们做不做 |
|---|---|---|---|
| **时间** | 环比 / 同比 | 趋势与季节性剥离 | **环比做**;分类同比**数据不够**(见 §4) |
| **结构** | 刚性 / 弹性占比 | 判断财务刚性程度 | **做** —— 这是主线 |
| **对象** | 商家集中度 / 复购频次 | 发现隐性浪费 | **不做** —— 没有商户字段(见 §4) |
| **周期性** | 订阅识别 / 年费提醒 | 防无感扣款 | **做** |
| **结余** | 结余率 = (收入−支出)/收入 | 现金流健康度 | **已有**(储蓄率) |

其中一条经验法则值得单独记:
**弹性支出适合看环比**(响应快、可调整),**固定支出适合看同比**(变化慢,阶梯式上涨只有同比看得出)。

### 1.8 · 调研结论 → 对我们 PRD 的四条直接影响

1. **不新建页面,补进报表页。** Monarch 的 Month in review 证明了「支出 + 现金流 + 净资产」
   本来就该在一份报告里,而我们那份报告已经存在 —— 缺的只是支出那一块的深度。
   (顶栏已有 7 个 tab,加第 8 个的代价远大于收益。)
2. **三分法是对的,但用途和竞品不同。** 他们用来定预算,我们用来**修分母**。
   这是我们唯一能做而他们做不到的事 —— 因为只有我们同时握着净资产、流动资产和 FIRE 目标。
3. **订阅识别是中文市场空白。** 三个中文 App 一个都没做,两个海外产品都做了且都放在显眼处。
4. **一条都不要做价值判断。** 少数派那句「花钱永远没有对错」是这一版所有文案的红线。

---

## 2 · 用户故事

维护者原话(2026-09-20):

> 我们在上个版本中增加了详细的支出明细的部分;但是针对这部分数据 **价值获取分析没有做**;
> 我们现在启动下一个迭代,目标是**如何帮助用户进行自己家庭支出的分析**。

拆成四个可验收的场景:

**S1 · 这个月为什么多花了(非技术家人也要看得懂)**
> 妻子打开报表页,看到「本期比上期多花 ¥4,260」。她想知道是哪儿多的。
> 今天:她得自己把两个月的类目列表并排比。
> 期望:**一眼看到 `旅游 +¥3,900 · 餐饮 +¥620 · 交通 −¥260`**,而且知道旅游那笔是一次性的。

**S2 · 我的紧急储备到底够几个月(维护者)**
> 「8.2 个月」这个数是拿去年那笔装修一起平均出来的。
> 期望:**页面同时给两个数** —— 「按全部支出 8.2 月 / 剔掉一次性后 11.6 月」,
> 并且能点开看到被剔掉的是哪几笔。

**S3 · 有没有在悄悄漏钱(维护者)**
> 几个视频会员、一个云盘、一份保费,每月自动扣。没人会主动去查。
> 期望:**一张「每月都在扣」的清单**,合计多少、占月支出多少、哪个涨价了。

**S4 · 砍到什么程度才够(维护者 · 服务 FIRE 目标)**
> FIRE 目标按现在的支出算要 N 年。如果只保留刚性支出,要几年?
> 期望:**刚性 / 弹性 / 一次性的三分占比**,让「能砍的空间有多大」变成一个数而不是一种感觉。

---

## 3 · FR

> 编号从 FR-610 起(v1.22 用到 FR-601)。
> 分组 A 是数据基础,B/C/D/E 是四块读的能力,F 是回流,G 是零影响。

### 3.1 分组 A · 支出性质三分(FR-610~616)

| # | 要求 |
|---|---|
| **FR-610** | 每个**一级类目**有一个「支出性质」标记,三选一:**刚性** / **弹性** / **一次性**。二级类目默认继承父级,可单独改 |
| **FR-611** | 起步包(v1.21 FR-509)预设好性质,用户**不做任何事**也有一份合理的默认:<br>**刚性** = 居住(房租房贷)/ 生活缴费 / 通讯 / 保险 / 教育 / 医疗<br>**弹性** = 餐饮美食 / 交通出行 / 购物消费 / 休闲娱乐 / 人情往来<br>**一次性** = 旅游 / 装修 / 大额家电 / 其他 |
| **FR-612** | 性质可改,入口在类目管理页(与 v1.21 的类目树共用一屏),**一屏改完 10 个大类**。改动**立即对全部历史生效**(它是类目的属性,不是笔的属性) |
| **FR-613** | **逐笔可覆盖**:单笔支出有一个「**这笔是一次性的**」勾,默认**不勾**。用于「弹性类目里的一笔大额」(例:购物类目下买了一台 ¥12,000 的电脑)。逐笔录入与账单导入**两条路都要有**(L13 纪律:写入口都要守) |
| **FR-614** | 逐笔勾**只能勾成「一次性」**,不能把一次性类目里的笔改回弹性。理由:三个值全都可逐笔覆盖 = 三倍的判断负担,而实际需求只有「这笔特殊,别算进月均」这一个方向 |
| **FR-615** | 「一次性」的判定是**类目性质 = 一次性 OR 该笔被勾了一次性**。这条判据只存在于**一处**(一个 service 方法),所有读它的地方调同一个方法 |
| **FR-616** | 没设过性质的类目(老用户自建的类目)默认**弹性**。理由:弹性是三者里**最不改变任何现有数字**的那一个(常态月均 = 月均支出) |

### 3.2 分组 B · 支出归因:这个月为什么变了(FR-620~625)

| # | 要求 |
|---|---|
| **FR-620** | 报表页支出章节新增**「比上期」瀑布**:左端上期总支出,中间每个类目的增减条(增红减绿),右端本期总支出。与报表页已有的「资金流瀑布」用**同一种视觉语言** |
| **FR-621** | 瀑布**只显示前 N 个变化最大的类目**(默认 5),其余合并成「其他 N 项合计」。全量在下方表格里 |
| **FR-622** | 每个条上标注**金额差**和**变化率**;金额差是主信息(大字),变化率是次信息 —— 「餐饮从 ¥100 涨到 ¥200」的 +100% 不如「旅游多了 ¥3,900」重要 |
| **FR-623** | **一次性支出在瀑布里单独成段**并用斜纹填充,紧挨着标注「一次性 · 不进常态月均」。这是把 S1 和 S2 串起来的那一笔 |
| **FR-624** | **总额环比 + 同比**一行给出(同比 = 去年同月)。同比在数据不足 13 期时显示「攒够 13 期才有」,**不显示 0 也不显示空白** —— 承 v1.21 FR-519 的纪律:**没数据 ≠ 没花钱** |
| **FR-625** | 上期是 **TOTAL 模式**(只填了总额没有分类)时,瀑布**不画**,改为一行说明「上期只填了总额,拆不开」。不许拿 0 当上期各类目的值 —— 那会让每个类目都显示成「新增」 |

### 3.3 分组 C · 刚性 / 弹性 / 一次性 分层(FR-630~636)

| # | 要求 |
|---|---|
| **FR-630** | 支出章节新增**「这些钱砍得掉吗」**分层块:三段堆叠条(刚性 / 弹性 / 一次性),各段标注金额与占比 |
| **FR-631** | 同时给 **12 期的刚性/弹性趋势**(两条线或堆叠柱)。刚性线应该是平的 —— **它一旦抬升就是真的生活成本上台阶了**,这是同比才看得出的那类变化 |
| **FR-632** | **常态月均**:剔除一次性后的近 12 期月均,与既有 `月均支出` **并排显示**,两个数都给,并说明差额来自哪几笔 |
| **FR-633** | **一次性明细可下钻**:点常态月均旁的「剔除了 N 笔」→ 展开那 N 笔(日期/类目/金额/是类目性质还是逐笔勾) |
| **FR-634** | **刚性占比**单独作为一个指标呈现,并给一句口径说明:「刚性占比越高,家庭现金流的可压缩空间越小」。**不给好坏评价**,不设阈值红绿灯(承 §1.6 的「花钱没有对错」) |
| **FR-635** | 三分数据不足时(启用不满 3 期、或 90% 的笔都未分类)→ 整块**不渲染**,给一行「再记两个月就能看了」。不渲染一个全是「未分类 100%」的块 |
| **FR-636** | 「未分类」的笔(`expense_category_id IS NULL`)在三分里算作**弹性**,并在块下注明有多少笔未分类 + 给「去归类」入口。**不许把未分类偷偷丢掉**(L13 硬约束③) |

### 3.4 分组 D · 周期性支出(FR-640~645)

| # | 要求 |
|---|---|
| **FR-640** | 支出章节新增**「每月都在扣」**清单:自动识别出的周期性支出,按月扣款额降序 |
| **FR-641** | 识别判据(**工程算,不是 AI 算**):同一**类目**下,金额落在同一个 ±5% 区间内的笔,**连续出现 ≥ 3 期**。`note`(导入时写的是商户名)作为**展示标签**,不参与判定 —— 它是自由文本,手填和导入混在一起,不可靠 |
| **FR-642** | 每条给出:标签 / 月均金额 / **已连续 N 期** / 最近一期金额。**金额变了要标出来**(承 Copilot 的 subscription creep):「9 月起从 ¥15 涨到 ¥25」 |
| **FR-643** | 清单给一个**合计**:「每月固定扣款 ¥X,占月均支出 Y%」 |
| **FR-644** | 识别结果**可以「不是订阅」一键忽略**,忽略后不再出现。误判成本低但反复出现很烦 |
| **FR-645** | **不做任何「去取消」的引导**,不接第三方退订。我们只负责**让它被看见** |

### 3.5 分组 E · 支出结论回流(FR-650~654)

> 这一组是「让支出参与决策」的落点。**每一条都必须是并列读数,不许改既有数字。**

| # | 要求 |
|---|---|
| **FR-650** | **紧急储备月数**的 tooltip / 详情里增加一行并列读数:「按常态月均 = **11.6 月**」。**豆腐块上的主数字不变**(仍是 8.2),避免所有现存用户的数字一夜之间改变 |
| **FR-651** | **体检页 · 流动性维度**同样并列给出两个读数,并说明差额由 N 笔一次性支出造成 |
| **FR-652** | **目标页 · 应急金**:目标额按两种月均各算一次,并排显示「按全部支出需要 ¥A / 按常态月均需要 ¥B」。用户**自己**决定按哪个准备 —— 不替他选 |
| **FR-653** | **AI 月报**的输入里增加支出结构事实(三分占比、环比归因前三、周期性合计)。**所有数字工程算好填进 prompt**,AI 只写一句人话(承 `feedback_llm_no_math`)。AI 不可用时该段不出现,不影响月报其余部分 |
| **FR-654** | **dashboard 不加任何新组件。** 它已经很满了,而支出分析是「每月看一次」的东西,不是「每天看一眼」的东西 |

### 3.6 分组 F · 页面组织(FR-660~663)

| # | 要求 |
|---|---|
| **FR-660** | 全部新内容落在**报表页**已有的「支出构成」区,把它扩成一个完整章节。**不新建 tab、不新建路由** |
| **FR-661** | 章节内顺序按「**先结论后明细**」:① 花了多少·比上期(环比+同比数字条)② 为什么变(归因瀑布)③ 砍得掉吗(三分+常态月均)④ 每月都在扣(周期性)⑤ 花在哪(现有构成)⑥ 年度累计 ⑦ 12 期趋势 |
| **FR-662** | **长文目录 TOC 必须同步**(联动链 L4):新 section 的 `id` + `fragments/_toc` 条目 + `static/js/toc.js`。新增的是**子节**(`sub:true`),挂在支出构成之下 |
| **FR-663** | 移动端:瀑布与三分条**纵向堆叠**,不横向压缩。并列元素尺寸必须一致(`feedback_sibling_uniform_selfcheck`) |

### 3.7 分组 G · 零影响(FR-670~672)

| # | 要求 |
|---|---|
| **FR-670** | **TOTAL 模式的家庭**(只填月度总额)看到的支出章节与上一版**逐字一致**,新块一个都不出现。他们没有分类数据,所有新块都无从算起 |
| **FR-671** | **既有指标的数字一个都不变**:紧急储备、储蓄率、月均支出、FIRE 目标、人赚钱赚、XIRR 全部逐字不变。新读数一律是**并列增加**。验收要有**零差异基线**(§6.2) |
| **FR-672** | 没有任何逐笔数据的家庭(v1.21 之前的老数据)→ 新块按 FR-635 的规则不渲染,不报错、不显示 0 |

---

## 4 · 不做什么

### 非目标(这一版刻意不做)

- **预算 / 超支预警 / 日历红蓝标** —— `AGENTS.md` §1 明文「刻意不做预算包络」,这一版不推翻。
  理由见 §0.4:预算是月度自律工具(每天看),我们是资产决策工具(每月看一次)。
  Copilot Money($95/年,海外支出分析最强的一个)**主动放弃了预算**,这是一个独立佐证。
- **任何价值判断的文案** —— 不说「冲动消费」「你这个月花超了」「建议减少」。
  承少数派那句「**花钱永远没有对错**」。系统的职责是**指出变化**,不是**评判选择**。
  唯一的例外是纯事实陈述(「9 月起从 ¥15 涨到 ¥25」)。
- **商家 / 交易对象维度**(MoneyWiz 的「品牌消费集中度」)—— 我们**没有商户字段**。
  导入时商户名被 `trimNote()` 塞进了 `cash_flow.note`(`BillCommitService.java:175`),
  而 `note` 同时装着手填备注。在一个混了两种来源的自由文本上做聚合,
  产出的排行榜**看起来很专业但是错的**。要做得先加一个真正的 `counterparty` 列 —— 那是下一版的事。
- **分类同比** —— 需要 13 期的分类数据,而 v1.21 才发布不到一个月。做了也是一年的空白。
  **总额同比可以做**(PMC 从 v0.1 就有),见 FR-624。
- **记账习惯激励**(打卡 / 连续天数 / 徽章 / 财富币)—— 鲨鱼记账有一整套,那是留存设计。
  我们的用户每月来一次,不需要被激励留存。
- **消费建议 / 省钱攻略 / 优惠券** —— 不是我们的轴。
- **自定义统计周期**(钱迹的「自定义时间段」/ 随手记的「自定义月起始日」)——
  我们的账期是一等公民且 v1.23 刚把它的边界做成可配置的,再叠一层独立的统计周期
  会造成两套时间口径。**分析周期 = 账期**,不给第二个。

### 推迟到后续版本

- **商户字段 + 商家集中度**:要加 `cash_flow.counterparty` 列并改导入写入路径,值得单独一版
- **分类同比**:攒够 13 期自动可用,到时候只是放开一个显示开关
- **支出预测**(下个月大概花多少):有了刚性/弹性分层之后才有意义,但预测要背准确性的责任,想清楚再做

---

## 5 · 10 分钟约束核对

| 动作 | 频次 | 耗时 | 说明 |
|---|---|---|---|
| 设置类目性质(FR-610~612) | **一次性**,之后基本不动 | **~60 秒** | 10 个大类一屏点完;**而且有预设默认,不做也能用** |
| 勾「这笔是一次性的」(FR-613) | 极低频(大额时才用) | **~1 秒/笔** | 默认不勾;一年可能用 3-5 次 |
| 「不是订阅」忽略(FR-644) | 极低频 | ~1 秒/条 | 只在误判时 |
| **读分析** | 每月一次 | 0 | 本版**全部是读**,不增加任何录入 |

**结论:新增录入负担 ≈ 0。** 这一版的全部数据来源都是 v1.21/v1.22 已经存在的逐笔流水 ——
它不是「记更多东西」,是「**把已经记下的东西用起来**」。少数派那句
「没有统计数据的输入,哪来的数据分析输出」反过来也成立:**输入已经付过成本了,不用就是浪费**。

---

## 6 · 存量影响

| 维度 | 这一版的影响 |
|---|---|
| **schema** | **只加不改**:`expense_category` 加 1 列(`expense_nature` VARCHAR(8) NULL);`cash_flow` 加 1 列(`one_off` TINYINT(1) NOT NULL DEFAULT 0)。既有 377+ 行一个字节不动。两列都向前兼容老 jar(L7) |
| **口径** | **本版不涉及口径变更** —— 详见 §6.2 |
| **UI** | TOTAL 模式家庭 / 无逐笔数据家庭:**逐字一致**(FR-670/672)。有逐笔数据的家庭:支出章节变长,既有三块**位置和内容都不变**,新块加在**前面** |

### 6.1 新增的两列怎么填

| 列 | 老行怎么办 | 为什么安全 |
|---|---|---|
| `expense_category.expense_nature` | 迁移里按**起步包名字**批量填(FR-611);自建类目留 NULL | NULL 读作「弹性」(FR-616),而弹性 = 不改变任何既有数字 |
| `cash_flow.one_off` | `DEFAULT 0` | 0 = 不是一次性 = 既有行为 |

### 6.2 口径变更声明

**本版不涉及口径变更。** 这不是省略,是一个**刻意的设计选择**,理由写清楚:

「用常态月均替换月均支出」是这一版**最有价值也最危险**的一件事。替换会让每个现存用户的
紧急储备月数、应急金目标、体检流动性评分**在升级的那一刻全部改变**,而他们没做任何事。
`feedback_metric_refactor_baseline` 的教训就是这类改动。

所以本版采取**双读数**:既有数字**逐字不变**,常态月均作为**并列的第二个读数**出现
(FR-650~652)。用户自己判断哪个更贴近他家的情况 —— 这比我们替他决定更诚实,
**而且两个数摆在一起本身就是信息**(差多少 = 一次性支出对判断的干扰有多大)。

**零差异基线怎么建**:
1. 基线来自**已发布的 tag `v1.23.0`**(不是当前 master —— 否则分不清代码差异和数据漂移)
2. 在 beta 快照上,用 v1.23.0 的 jar 跑一遍,抓下 dashboard / 报表 / 体检 / 目标四页的
   全部既有 KPI 数值
3. 换 v1.24 的 jar,同一份快照,同样抓一遍
4. **既有 KPI 必须逐字相等**;有任何一个不等就停下来查因,不许「大概是四舍五入」
5. 落成 `scripts/regression-data.sh` 的一组断言 + 一条 qa-run 护栏
   `v1240-EXISTING-KPI-UNCHANGED`(扫 `averageExpense` 的实现没有读 `one_off` / `expense_nature`)

**调用方清单**(靠人列必漏,所以配 grep 护栏):
`FactViewServiceImpl.averageExpense:1130` 是唯一的计算点,消费方 4 处 ——
`DashboardController:324,423` · `CheckupController:80,150` · `ReportsController:591` · `LiquiditySurplus:41`。
护栏守的是「**这个方法不许读新列**」,比守「消费方没变」更靠得住。

### 6.3 核心功能影响排查

**触发条件命中**:加了新的**写入通道**(逐笔 `one_off` 勾,两条路)+ 引入了一个此前不存在的
**正交维度**(支出性质)。以下按模板三步走。

#### ① 全部写操作 × 归属判据

新增/受影响的写操作只有 4 个(v1.23 §4 已经枚举过全部 27 个,这里只列本版新增和变化的):

| # | 写操作 | 入口 | 归属判据(L16) | 说明 |
|---|---|---|---|---|
| W1 | 逐笔录入时勾「一次性」 | 填报页 · `EntryService` | **显式选期** | 跟着那一笔走,笔在哪期就在哪期 |
| W2 | 导入确认页勾「一次性」 | 导入页 · `BillCommitService` | **显式选期** | 同上;账单行的日期决定落哪期(v1.21 已有逻辑) |
| W3 | 改类目的支出性质 | 类目管理页 | **无期归属** | 它是**类目的属性不是笔的属性** —— 改了立即对全部历史生效。这是有意的:「房贷一直都是刚性」是一个事实陈述,不是一个从今天起生效的决定 |
| W4 | 「不是订阅」忽略 | 报表页 · 周期性块 | **无期归属** | 家庭级偏好,不写 `cash_flow` |

**注意 W3 的性质**:它是**唯一一个会改写历史展示的写操作**。改了之后,去年 1 月的三分占比
也会跟着变。这**是对的**(类目性质是一个事实,不是一个版本),但**必须在 UI 上说清楚**:
改性质的确认文案要写「这会同时改变历史各期的分层结果」。

**对 v1.23 双活跃账期的影响**:W1/W2 跟着笔走,笔的归属 v1.23 已经定好了,**本版不引入新的取期决策**。
W3/W4 不写 `cash_flow`,与账期无关。**本版没有任何 `findCurrentOpen()` 式的隐式取期。**

#### ② 三个核心页面的组件 × 锚点

只列**本版触碰或新增**的;未列出的组件本版**确定不受影响**,理由是它们不读 `one_off` /
`expense_nature` / 不读支出分类聚合(护栏 `v1240-EXISTING-KPI-UNCHANGED` 守这一点)。

| 页 | 组件 / 指标 | 锚点 | 本版 |
|---|---|---|---|
| 报表 | 支出构成 · 本期构成 | `anchorPeriod` 点值 | 不变(位置下移) |
| 报表 | 支出构成 · 年度累计 / 12 期趋势 | 窗口累计 | 不变 |
| 报表 | **环比 + 同比数字条** | `anchorPeriod` vs 上期 / 去年同月 | **新增** |
| 报表 | **归因瀑布** | `anchorPeriod` vs 上期 | **新增** |
| 报表 | **三分 + 常态月均** | 近 12 **已定稿**期(与 `averageExpense` 同窗口) | **新增** |
| 报表 | **周期性清单** | 近 12 已定稿期 | **新增** |
| 报表 | 储蓄率 / 月均支出(`_savings`) | 窗口累计 | **不变**(FR-671) |
| dashboard | 紧急储备豆腐块 | 实时分子 ÷ 12 期均值 | **主数字不变**,tooltip 加一行 |
| dashboard | 其余全部 | — | **不变**(FR-654:不加组件) |
| 填报 | 逐笔支出行 | 显式选期 | 加一个勾(FR-613) |
| 体检 | 流动性维度 | 同 dashboard | **评分不变**,说明加一行 |
| 目标 | 应急金目标 | `LiquiditySurplus` | **主数字不变**,并排加第二个 |

#### ③ 矩阵:组件 × 操作

列按「对数据的影响形态」归并成 5 类:
**A** = 新增/改一笔支出 · **B** = 勾/取消「一次性」 · **C** = 改类目性质 · **D** = 改类目树(v1.21 已有) · **E** = 关账 / 开新期(v1.23)

图例:`↑` 立即变 · `—` 不变 · `⏳` 到某事件才变

| 组件 / 指标 | A | B | C | D | E |
|---|---|---|---|---|---|
| 本期构成 | ↑ | — | — | ↑ | — |
| 年度累计 / 12 期趋势 | ↑ | — | — | ↑ | — |
| **环比 · 同比数字条** | ↑ | — | — | — | ⏳ |
| **归因瀑布** | ↑ | ↑ | — | ↑ | ⏳ |
| **三分堆叠 + 12 期分层** | ↑ | ↑ | ↑ | ↑ | ⏳ |
| **常态月均** | ⏳ | ⏳ | ⏳ | — | ⏳ |
| **周期性清单** | ⏳ | — | — | ↑ | ⏳ |
| 紧急储备(主数字) | ⏳ | **—** | **—** | — | ⏳ |
| 紧急储备(常态读数) | ⏳ | ⏳ | ⏳ | — | ⏳ |
| 储蓄率 / 月均支出 | ⏳ | **—** | **—** | — | ⏳ |
| 人赚钱赚 / XIRR / 净资产 | ⏳ | **—** | **—** | — | ⏳ |

**读出来的三条规律**(读不出规律说明抽象没找对):

1. **B 列和 C 列在「既有指标」那几行全是 `—`。** 这正是 §6.2 的双读数承诺的机器表达:
   一次性标记和支出性质**只被新指标读**。这条规律就是护栏 `v1240-EXISTING-KPI-UNCHANGED`
   要守的东西 —— 一旦哪天有人让 `averageExpense` 读了 `one_off`,这一列会出现 `↑`,护栏会红。
2. **常态月均那一行全是 `⏳`。** 因为它锚**已定稿**期(与 `averageExpense` 同窗口,L12/L16),
   进行中账期的改动要等那期定稿才进均值。**这一行和紧急储备主数字的形态完全一致** ——
   它们本来就该同步,并排显示的两个数如果更新节奏不同,用户会以为算错了。
3. **C 列只在「分层类」组件上是 `↑`,在「金额类」组件上全是 `—`。**
   改类目性质**不改变任何一分钱的归属**,只改变这些钱**被分进哪一堆**。
   这是「性质是第三个正交维度」这句话的直接后果。

#### ④ 明确不受影响的边界

以下**确定不受影响**,理由不是「没查」而是「查了,它们的取数里没有支出分类这一层」:

- **全部存量类指标**(净资产 / 总资产 / 总负债 / 流动资产 / 配置环 / 环比同比资产)——
  读 `period_snapshot.end_balance`,与 `cash_flow` 的分类字段无关
- **全部收益类指标**(XIRR / TWR / YTD / 本月资产收益 / 人赚钱赚)——
  读的是 `cash_flow` 的**金额与方向**,不读 `expense_category_id`,更不读新增两列
- **账户余额 / 轧差 / `affects_balance` 那条链** —— 本版**一个字节都不碰**。
  `one_off` 是一个纯**分析期标记**,不参与任何余额语义(这一点要写进列注释,
  因为 `affects_balance` 已经有过一次「一个标记要同时退出三条链」的教训,L13 硬约束⑤)
- **v1.23 的双活跃账期全部判据** —— 本版不新增任何取期逻辑(见 ① 的结论)

---

## 7 · 开关与默认值

| 开关 | 默认值 | 关掉时的行为 |
|---|---|---|
| 支出分析章节 | **自动**(有逐笔分类数据就显示,没有就不显示) | 无数据 = 与上一版逐字一致(FR-670/672) |
| 类目支出性质 | **有预设**(FR-611),用户不设也能用 | 全 NULL → 全算弹性 → 三分块退化成一条弹性;**常态月均 = 月均支出** |
| 逐笔「一次性」勾 | **不勾** | 既有行为 |
| 紧急储备常态读数 | **开**(但只出现在 tooltip / 详情,主数字不变) | — |
| AI 月报支出段 | 跟随既有 AI 开关 | AI 不可用 → 该段不出现,月报其余部分不受影响 |

**这一版没有「改变既有口径」的开关,因为没有改变既有口径。**
按模板的规矩,「改变既有口径或既有视图的能力默认必须关」—— 我们用「不改变」绕开了这条,
代价是用户要自己看两个数。这是刻意的取舍,理由见 §6.2。

---

## 8 · 隐私与数据红线

- **AI 只接收已经算好的聚合数字和类目名**(FR-653),不接收逐笔明细、不接收商户名、
  不接收 `note`。承 `feedback_llm_no_math`:**AI 不做任何数学**,所有数字工程算好填进 prompt
- **周期性识别全在服务端工程代码里做**(FR-641),不调 LLM。判据是确定性的,可复现、可解释,
  出了问题能指到代码;LLM 做这件事既不可复现也解释不了
- **prod 真实金额不落任何文件**(`feedback_prod_data_never_persisted`):
  本 PRD、preview、截图、Release 页、commit message 里的金额**全部是编造的示例值**;
  验收时如需记录算式关系,用 «A» «B» 符号
- 本版**不新增任何对外接口**,不接任何外部平台

---

## 9 · 失败模式(pre-mortem)

> 假设这一版发出去砸了,最可能是以下哪一条。

| # | 失败模式 | 为什么会发生 | 防法 |
|---|---|---|---|
| **F1** | **既有指标数字悄悄变了**,用户发现紧急储备从 8.2 变成 11.6 却没人告诉他 | 实现时图省事,直接让 `averageExpense` 读 `one_off` —— 改一行代码就「做完了」,而且测试全绿(因为测试数据里没有一次性笔) | §6.2 零差异基线(来自 tag `v1.23.0`)+ 护栏 `v1240-EXISTING-KPI-UNCHANGED` |
| **F2** | **三分块全是「未分类 100%」** —— 老用户打开看到一个毫无信息的块 | 起步包只对**新建**的类目生效,老用户的自建类目全是 NULL | FR-616(NULL 读作弹性)+ FR-635(数据不足不渲染)+ FR-636(未分类要显式说出来) |
| **F3** | **归因瀑布把每个类目都显示成「新增」** | 上期是 TOTAL 模式,代码拿 0 当上期各类目的值 | FR-625 显式拦住;e2e flow 要**造一个 TOTAL 上期**来验 |
| **F4** | **周期性识别一堆误判**,把每月都去的那家超市认成订阅 | 判据太松(只看「连续出现」不看「金额恒定」) | FR-641 金额 ±5% 是硬条件 + FR-644 可一键忽略 |
| **F5** | **改类目性质之后历史数字变了,用户以为出 bug** | W3 是唯一会改写历史展示的写操作,而这件事不直观 | §6.3 ① 要求确认文案明写「会同时改变历史各期的分层结果」 |
| **F6** | **文案滑向价值判断** —— 某个实现者顺手写了「餐饮偏高,建议控制」 | 这类文案写起来最顺手,而且看起来「更有用」 | §4 红线 + qa-run 护栏 `v1240-NO-JUDGEMENT`(扫模板里的「建议减少/偏高/超支/冲动/节省」等词) |
| **F7** | **TOC 锚点漏同步**,新 section 点不到 | L4 是老坑,已经踩过 | FR-662 + 既有护栏 `v05-TOC-1/2/3` |
| **F8** | **移动端瀑布被压成一坨** | 瀑布是横向图表,手机宽度不够 | FR-663 纵向堆叠 + 双端截图验收(`feedback_ui_ued_review`) |

---

## 10 · 验收 → 护栏映射

| 验收点 | 怎么验 | 护栏 |
|---|---|---|
| 既有 KPI 逐字不变 | 零差异基线(v1.23.0 tag vs v1.24)四页全 KPI | `v1240-EXISTING-KPI-UNCHANGED`(静态:`averageExpense` 不读新列)+ `regression-data.sh` 断言 |
| 一次性判定只有一处 | grep 判定逻辑只出现在一个 service 方法 | `v1240-ONEOFF-SINGLE-JUDGE` |
| `one_off` 不碰余额链 | grep `one_off` 不出现在 `applyDeltaToBalance` / 轧差 / NAV 路径 | `v1240-ONEOFF-NOT-BALANCE` |
| 未分类不被丢掉 | 三分合计 == 本期消费总额 | `v1240-UNCLASSIFIED-IN-SPLIT` + 单测 |
| TOTAL 上期不画瀑布 | e2e:造一个 TOTAL 上期,断言瀑布不出现且说明出现 | e2e flow `22-expense-analysis.cjs` |
| 无数据不渲染 | e2e:清空分类数据,断言新块全部不出现 | 同上 |
| 文案无价值判断 | 扫模板里的判断词 | `v1240-NO-JUDGEMENT` |
| TOC 同步 | 新 section id 在 `_toc` 里有条目 | `v05-TOC-1/2/3` |
| 双端渲染 | PC + 移动截图,并列元素同尺寸 | 人工 UED review + `feedback_sibling_uniform_selfcheck` 四条自查 |
| 真 e2e 走用户路径 | 顶栏「报表」→ 滚到支出章节 → 点下钻 → 点「不是订阅」 | `scripts/e2e/flows/22-expense-analysis.cjs` |

---

## 11 · 回滚与关闭

- **回滚 = 回 jar 即可**。两个新列都是可空/有默认值,老 jar 见到它们照常跑(L7)
- **数据不需要回滚**:`one_off` / `expense_nature` 是纯分析期标记,没有任何金额依赖它们
- **关闭 = 无数据**:没有逐笔分类数据的家庭本来就看不到新块,不需要额外开关
- **最坏情况**(某个新块算错):该块可以单独摘掉,其余块与既有三块不受影响 ——
  因为它们之间没有数据依赖,各自独立从 `cash_flow` 聚合

---

## 12 · 来源与参考

| # | 来源 | 用在哪 |
|---|---|---|
| 1 | [鲨鱼记账 · 官网](http://www.shayujizhang.com/) | §1.1 功能清单(预算/日历/导出/定时记账) |
| 2 | [鲨鱼记账产品体验报告 · 站酷](https://www.zcool.com.cn/article/ZOTYzMjM2.html) | §1.1 页面结构、记账 2 步流程、图表页拆解 |
| 3 | [鲨鱼记账 · 图表页体验(检索汇总)](https://m.ui.cn/details/525983) | §1.1 周/月/年切换 + 支出排行 + 占比上升红标 |
| 4 | [钱迹 · 官网](https://qianjiapp.com/) | §1.2 统计报表 / 资产管理 / 多平台导入 |
| 5 | [钱迹 · 少数派测评](https://sspai.com/post/61668) | §1.2 标签体系、预算、资产负债表 |
| 6 | [随手记 · 官网](https://www.sui.com/) | §1.3 五种报表、首页即报表、情景账本 |
| 7 | [随手记竞品分析 · 人人都是产品经理](https://www.woshipm.com/evaluating/810827.html) | §1.3 定位与用户诉求分层 |
| 8 | [Copilot Money Review 2026 · Finny](https://getfinny.app/blog/copilot-money-review-2026) | §1.4 分类准确率、Recurring 自动识别、回溯型定位 |
| 9 | [Copilot vs Monarch vs WalletHub (2026)](https://wallethub.com/edu/b/copilot-vs-monarch-vs-wallethub/148207) | §1.4/1.5 两者分工、订阅检测、Flex 预算三分 |
| 10 | [About Monarch's AI Features · Monarch Help](https://help.monarch.com/hc/en-us/articles/16116906962452-About-Monarch-s-AI-Features) | §1.5 Weekly Recap / Insights / Month in review |
| 11 | [如果不做报表分析,那你每天记账也可能只是白费功夫 · 少数派](https://sspai.com/post/34313) | §1.6 2+2 报表框架、交易对象维度、「花钱没有对错」 |
| 12 | [同比环比分析 · 简道云文档](https://hc.jiandaoyun.com/doc/11561) | §1.7 环比/同比口径 |
| 13 | [8 款好用记帐软件详细比较 · Ragic](https://www.ragic.com/intl/zh-CN/blog/448/best-expense-tracker-apps) | §1.7 免费/付费能力边界 |

**代码依据**(§0.1 与 §6 的每个结论都指到行):

- `FactViewServiceImpl.java:126-129` 紧急储备 = 流动资产 ÷ 月均支出
- `FactViewServiceImpl.java:1130-1157` `averageExpense` 实现(近 12 已关账期扁平平均)
- `LiquiditySurplus.java:41` 应急金目标 = 月均支出 × N
- `CheckupController.java:80-96,150` 体检流动性维度取同一个月均
- `ReportsController.java:591` 报表储蓄率的月均支出
- `DashboardController.java:324,423-431` dashboard 两处消费
- `BillCommitService.java:175` 导入时商户名 → `trimNote()` → `cash_flow.note`(§4 不做商家维度的依据)
- `reports/_expense-cat-mix.html`(131 行)现有支出构成三块
- `db/migration/V59__expense_category_split.sql` v1.21 的三表三列

---

## 13 · 待维护者裁决

1. **双读数 vs 直接换口径**(§6.2)。我选了双读数(既有数字不变,并列加一个)。
   代价是用户要自己看两个数;收益是没有任何现存用户的数字会一夜之间改变。
   如果你认为「常态月均本来就是更对的那个口径,该直接换」,那就是一次**真正的口径变更**,
   要按 `feedback_metric_refactor_baseline` 走完整流程(默认关 + 零差异基线 + 页面写明口径期)。
2. **逐笔「一次性」勾是否值得做**(FR-613)。它是本版唯一增加录入动作的东西(一年 3-5 次),
   但没有它,「购物类目里买了台电脑」就只能靠改整个类目的性质来处理 —— 那是错的。
3. **周期性识别的判据**(FR-641)。我定的是「同类目 + 金额 ±5% + 连续 ≥3 期」。
   没有商户字段,这是能做到的最好判据,但对「每月都去的超市 + 金额恰好稳定」会误判。
   可选:把门槛提到 4 期,准确率高一点、但新订阅要 4 个月才被发现。
4. **章节顺序**(FR-661)是「先结论后明细」。也可以保守一点把新块加在现有三块**后面** ——
   那样老用户的阅读习惯完全不变,但「所以呢」又排到了「摆出来给你看」的后面。
