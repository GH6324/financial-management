# v1.15 · 成员身份:登录名可改 · 归档 · 有条件删除

> 状态:**已评审通过**(2026-08-15 · agent 助手起草,来源 GitHub issue #12)
> §3 三个开放问题**作者已于 2026-08-15 全部拍板**,已回填进各 FR。
> 技术设计:[`tech-design/v1.15.md`](../tech-design/v1.15.md)(待评审 —— 通过后才动码)
> 目标版本:`app.version = 1.15.0`(已 bump)
> **版本号是占位**:本条与 v1.13(issue #14 · LLM 平台化)、v1.14(issue #11 · 拖拽上传)是**并行提案**,谁先获批谁就是下一个版本。作者定了顺序我改号即可,三边内容互不依赖。
> 前置:v0.1(成员/家庭模型)· v0.4.14(成员手机号)

---

## 0 · 这一版在解决什么

issue #12 原话是「要支持账户登录名称可改,目前似乎写死了两个,并不允许修改」,并追问「账户是否允许删除,历史上有过持仓账户的还能不能删,如果不能删至少增加一个归档?或者禁止登录怎么一个形式」。

**这是三件事,查完代码发现它们的性质完全不同**,分开做:

| 诉求 | 现状(已查证) | 这一版的性质 |
|---|---|---|
| 改登录名 | `members.html` 里输入框 `disabled`,底下写着「不可改」;`MemberMapper` 无任何 `username` 的 UPDATE | **推翻一个既有决定**,得先处理掉当初关它的理由 |
| 归档 / 禁止登录 | `member.archived_at` 从 V1 就有 · `Member.isArchived()` 有 · **登录拦截已实现** · 列表已按 `archived_at IS NULL` 过滤 | **补开关 + 善后**,不是从零做 |
| 删除 | 无任何删除入口 | **拆成两种情况**,不是一刀切 |

### 0.1 · 最值得说的发现:归档是一盏通了电但没装开关的灯

`MemberPrincipalService` 第 19 行已经在做 `.filter(m -> !m.isArchived())` —— 也就是 issue 里问的「禁止登录怎么一个形式」,**代码里早就是这个形式了**。

但全仓库**没有一条语句会把 `archived_at` 写进去**,页面上也没有任何入口。所以这个能力现在处于「只有直接改数据库才能触发」的状态。

**因此这一版的工作量不在那条 UPDATE,而在归档之后的善后**(§ FR-382)。

### 0.2 · 第二个发现:登录拦截只在「登录那一刻」生效

`MemberPrincipalService` 是 `UserDetailsService`,只在**认证时**被调用。项目里没有配 `SessionRegistry` / `maximumSessions`(见 `SecurityConfig`),所以:

> **把一个已经登录的人归档,他浏览器里那个会话会继续好用,直到自己过期。**

「禁止登录」如果只做到「下次登不进来」,在「孩子成年了要停掉他的访问」这类真实场景里是不成立的 —— 那个人现在就在用。所以 FR-381 把「当场失效」写成硬要求,而不是留给下一版。

---

## 1 · FR 清单

### FR-380 · 登录名可改

管理页的「登录名」输入框解除 `disabled`,可改并保存。

**格式与唯一性**:沿用创建成员时的规则(`[a-zA-Z0-9_]+`,3–40 字符)。唯一性校验必须**跨全表**,包括已归档成员 —— `uk_member_username` 是全表唯一约束,只查活跃成员会在保存时炸数据库异常而不是给出友好提示。

**必须一并处理的三件事**(这三件正是当初把它关掉的原因,不处理就是把坑打开):

1. **「记住我」令牌作废**。`persistent_logins` 这张表是**按 username 存**的(`series / username / token`),没有 member_id。改名后那些令牌指向一个不存在的用户名 —— 该成员在别的设备上的免登录会变成一张废票,而且是**静默**的废票。改名时必须删除该 username 名下的全部令牌,让他重新登录一次。
2. **当前会话跟随**。改的是自己的登录名时,会话里的 principal 还是旧 username,后续任何一次 `loadUserByUsername` 都会失败。要么当场刷新 principal,要么强制重新登录 —— 两个方案的取舍写进 TDD。
3. **审计留痕**。改登录名是身份变更,必须写 `audit_log`,且**记录旧名 → 新名**。只记「资料更新」等于把这次变更抹掉了。

**验收**:改名后 → 旧名登不进去、新名能登进去、该成员其它设备上的「记住我」失效需重新登录、`audit_log` 里能查到旧名和新名。

**注意(不是本 FR 要解决的,但要写明)**:项目目前**没有权限分级**,`SecurityConfig` 里除白名单外只有 `.anyRequest().authenticated()`,任何登录成员都能进 `/admin`。也就是说这一版之后,**任何家庭成员都能改别人的登录名**。

> **作者已拍板(2026-08-15):本版不引入权限分级**,就这么放着。理由是不发明半套权限模型 —— 半套比没有更难理解,要做就单独一版做完整。这是一条**明知并接受**的能力边界,不是遗漏;§2 非目标里同步登记。

### FR-381 · 归档入口 + 撤销 + 当场生效

管理页每个成员卡片给一个「归档」动作(二次确认),已归档的给「撤销归档」。写 `archived_at` / 置回 `NULL`。

**四条硬约束**:

1. **最后一个活跃成员不许归档**。`countActiveByFamily` 已经有了,直接用 —— 归档掉最后一个人 = 谁也登不进来,这个系统就废了。
2. **不许归档自己**(作者已拍板 2026-08-15)。当场把自己锁出去,而且因为不做权限分级(决定 3),救不回来 —— 只能直接改库。代价是「只剩一个人想停用系统」做不到,可接受:那个场景该做的是停服务,不是归档。**按钮对自己直接不渲染**,不要渲染出来再报错。
3. **当场生效**(§0.2 那个洞):归档后该成员**正在使用的会话必须立即失效**,而不是等它自己过期;「记住我」令牌一并删除。
4. **审计留痕**,归档和撤销都记。

**归档不影响钱**(作者已拍板 2026-08-15:归档后他名下的资产**照算**进家庭净资产)。查证结论是这条**零改动**即成立:全仓库的金额汇总(`CashFlowMapper` 各 `sum*` / `FactMapper.xml`)过滤的是 **`a.archived_at`(账户归档)**,`member` 只作 `LEFT JOIN` 取 `display_name`,没有任何一处按 `m.archived_at` 过滤。所以本 FR 的真正要求是一条**纪律**:归档实现里不许给任何汇总 SQL 补上 member 归档过滤。§4 的护栏钉这条。

**验收**:A 登录着,B 在另一浏览器把 A 归档 → A 下一次点击即被踢回登录页;A 用「记住我」也进不来;撤销归档后 A 能重新登录;归档前后家庭净资产、当期收入/支出合计**分毫不变**。

### FR-382 · 归档之后:该看见的地方看得见,不该出现的地方不出现

这是本版真正的工作量。`memberMapper` 的仅活跃查询(`findActiveByFamily` / `countActiveByFamily`)共 **9 处**调用点,**归档之后它们的正确行为各不相同**,已逐个查证:

| 调用点 | 用途 | 归档后应该 |
|---|---|---|
| `AdminController:213` | 成员管理页列表 | **必须能看见已归档的**,否则归档完人就消失了,连撤销按钮都点不到 |
| `AdminController:419` · `DashboardController:364` · `LensTagController:201` | 成员 id → 名字映射 | **必须含已归档的**,否则历史报表 / 透视页里那个人的名字变成空白 |
| `AccountController:181` · `AccountController:213` | 账户归属下拉 | **保持仅活跃** —— 面向未来的选择,不该再选到已退出的人 |
| `NotificationSettingsController:91,104` | 提醒对象 / 发送名单 | **保持仅活跃** —— 不能再给已归档的人发填报提醒短信 |
| `EntryController:142`(`totalMembers`)· `DashboardController:194`(`cfTotal`) | 「N/M 人已填报」的**分母** | **保持仅活跃** —— 已归档的人不该再被期待填报,分母该缩 |

> 后三行是这次复查补上的(`LensTagController` 的名字映射、以及两处**计数分母**)。第一版清单只列了 5 处,漏的正好是 memory `feedback_enum_add_sweep` 点名的两类:**藏在别的页面里的同类映射**和**写死的计数分母**。这也说明「调用点靠人列必漏」,所以护栏不能只钉这张表里的行号 —— 见 §4 第 6 条。

**要求**:新增一个「含已归档」的查询方法,**只给前两类用**,其余一行不动。默认值必须是安全的那一个 —— 即保留 `findActiveByFamily` 的现有语义,新方法另起名字;不要给旧方法加 boolean 参数(调用点上读不出 `findActiveByFamily(fid, true)` 是什么意思)。

管理页对已归档成员用视觉降级(灰化 + 「已归档」pill),不要做成两个列表 —— 两个列表会让「撤销」变得难找。

**一个已知偏差,本版不修**:分母那两处取的是「**今天**的活跃人数」,所以查看**已关账的历史账期**时,「N/M 人」的 M 会随今天的一次归档而变小。这与 v1.12.0「已关账月份不再变」的精神不一致,但它是**既有行为**(账户归档早就有同样问题,已由 `FactMapper.xml:186` 的 `a.archived_at > p.period_end` 单独处理),把成员侧一并做成「按期回溯应填人数」是另一件事,登记在 §2。

> **不要 emoji**,需要图形用项目自带的 Feather-style inline SVG(memory `feedback_no_emoji`)。

**护栏**:加一条 qa-run 守护,钉住「名字映射的两处调用点不得使用仅活跃的查询」。这类回归的特征是**不报错、只是名字变空**,人眼复看很容易放过去。

**验收**:归档一个填过历史数据的成员 → 历史报表里他的名字照常显示;账户归属下拉里没有他;提醒名单里没有他;管理页能看到他并能撤销。

### FR-383 · 删除:零引用才给删,且引用扫描不能靠外键

**有历史的不给物理删除**,理由写在 §2。**零引用的允许真删** —— 建错的账号(名字打错、多建一个)不该被迫在列表里躺一辈子,那是噪音不是历史。

**必须显式扫描引用,不能依赖数据库外键。** 已查证的引用位置:

**有外键的(9 处)**:`account.primary_owner_member_id` · `period_snapshot.submitted_by` · `cash_flow.submitted_by` · `transfer.submitted_by` · `snapshot_todo.assigned_member_id` · `snapshot_todo.done_by_member_id` · `period_member_completion.member_id` · `audit_log.actor_member_id` · `period_reopen_log.reopened_by`(V6)

**没有外键的(3 处 · 这是坑)**:`period_member_cashflow.member_id`(V19)· `stock_valuation_event.triggered_by_member_id`(V24)· 提醒发送记录的 `member_id`(V25)

**藏在 JSON 里的(1 处)**:`family_goal.params_json` 里 EDUCATION 目标的 `child_member_id`(V14)

后四处数据库**拦不住**,删了会静默产生孤儿行 —— 教育目标会指向一个不存在的孩子。所以扫描要逐个数,并把结果**显示在页面上**(「名下 N 个账户 · M 期填报记录 · K 条现金流」),让人看着数字做决定,而不是点了删除才被告知不行。

**一个要诚实说明的推论**:`audit_log.actor_member_id` 也算引用,所以**只要这个人登录后做过任何操作,就永远删不掉了**。这不是缺陷,恰恰是想要的结果 —— 能删的正好就是「建完还没用过」那一类,也就是 issue 里说的「建错了」。这一点要在页面文案里讲清楚,否则用户会以为删除按钮坏了。

**反过来的一条**:`audit_log` 里**以他为对象**的记录(`entity_type='member'`,比如「管理员创建了成员 X」)**不算引用**。那是「发生过什么」的流水,不是「他拥有什么」,删除后保留这条记录是正确的。

**二次确认**:删除要求输入该成员的登录名才放行(和常见的破坏性操作一致),并写 `audit_log`。

**验收**:新建一个成员立刻删 → 成功;让他登录一次再删 → 删除按钮不可用,并提示「他已经产生了 N 条记录,可以改为归档」;删除后 `period_member_cashflow` 等无外键表里不出现孤儿行(这条要有测试,不能只靠肉眼)。

---

## 2 · 非目标

- **不做权限分级 / 角色体系**(谁能管谁)—— **作者已拍板**。现在所有登录成员都能进管理页,这一版**不改这个现状**,只是把它写明(见 FR-380 末尾)。引入角色是另一件事,会牵动每个 `/admin` 页面,而且半套权限模型比没有更难理解。
- **不做「按账期回溯应填人数」**。「N/M 人」的分母用的是今天的活跃成员数,历史账期看起来会随归档变动(FR-382 末尾)。这是既有行为,不在本版扩大战线。
- **不把成员归档与资产口径挂钩**。归档后他名下的钱**照算**进净资产(作者已拍板),不提供「把某人那部分从家庭口径里摘出去」的开关 —— 那是「移出家庭」,是另一种语义、另一版。
- **不做成员合并**(把两个重复账号的历史并到一起)。听起来像是删除的替代,但它要重写历史归属,和「已关账月份不再变」(v1.12.0)直接冲突。
- **不做多家庭 / 邀请注册 / 自助注册**。成员由管理页创建,这条定位不变。
- **不改密码策略、不改登录方式**。
- **不做「删除时把历史数据一起删」的级联选项**。那是"把家底删掉一块",与项目定位冲突,任何形式都不提供。

---

## 3 · 作者已拍板(2026-08-15 · 三条全部确认)

| # | 问题 | 决定 | 落在哪 |
|---|---|---|---|
| 1 | 归档后他名下的钱还算不算净资产 | **照算** | FR-381「归档不影响钱」· §4 护栏 |
| 2 | 能不能归档自己 | **不能** | FR-381 硬约束 2 |
| 3 | 要不要顺手加权限分级 | **不加,本版维持现状** | FR-380 末尾 · §2 非目标 |

**决定 1 的实现代价是 0**,这是复查时查实的:所有金额汇总过滤的是**账户**归档(`a.archived_at`),`member` 只作 `LEFT JOIN` 取名字,没有一处按 `m.archived_at` 过滤。所以「照算」不需要写代码去实现,需要的是**别在实现归档时手滑给汇总补上 member 过滤** —— 这类「顺手加个过滤条件看起来更对称」的改动是最容易发生的,所以进护栏。

**决定 2 的连带**:按钮对自己**不渲染**(而不是渲染出来再拒),避免用户以为是 bug。

**决定 3 的连带**:本版之后,任何家庭成员都能改别人的登录名、归档别人。这是**明知并接受**的边界,不写进公开回复里当成特性讲,但也不隐瞒 —— issue 里已经说明过「项目目前没有权限分级」。

> 报告者当时被问到的「归档后钱算不算」的具体场景仍欢迎补充,但**不再阻塞本版** —— 若他答的是「孩子成年要摘出去」,那属于 §2 里明确不做的「移出家庭」,单独立项。

---

## 4 · 验收(整版)

1. 改名四条(旧名失效 / 新名可登 / 记住我作废 / 审计有旧名新名)。
2. 归档当场生效:已登录的会话被踢(FR-381 那条 A/B 双浏览器用例)。
3. 最后一个活跃成员、以及自己,都归档不了(自己那条:按钮根本不渲染)。
4. 归档后历史报表 / 透视页名字照常显示,账户归属与提醒名单里不出现他。
5. **归档前后家庭净资产与当期收支合计分毫不变**(决定 1)。
6. 零引用可删、有引用不可删;删除后无外键表里无孤儿行。
7. `mvn -o test` 全绿 · `bash scripts/qa-run.sh` 全绿,**新增三条护栏**:
   - **`v115-NO-MEMBER-ARCHIVE-IN-SUMS`**:金额汇总 SQL(`CashFlowMapper` / `FactMapper.xml` / 指标口径服务)里**不得出现按 member 归档过滤**的条件(`m.archived_at` / `member.archived_at`)。这条钉的是决定 1,属性级、扫全部汇总,不逐点补 —— 同 memory `feedback_currency_invariance` 的思路。
   - **`v115-MEMBER-NAME-MAP-INCLUDES-ARCHIVED`**:凡是把成员查询结果 `toMap` 成 id→名字的地方,都不得使用仅活跃查询。**钉模式不钉行号**(第一版清单就漏了 `LensTagController`),用「`findActiveByFamily` 后面跟 `toMap`/`put(...getId()`」这类结构判据。
   - **`v115-DELETE-SCAN-COVERS-FK-LESS`**:引用扫描必须覆盖那 3 张无外键表 + 1 处 JSON 引用(FR-383),钉「表名出现在扫描实现里」,防止以后加表漏扫。
8. 影响登录/会话,跑 `bash scripts/e2e.sh`。
9. 管理页 PC + 移动双端截图 + UED 复看(归档态的灰化不能让人以为是禁用/加载中)。
10. **向后兼容自检**(prod 已上线):本版**不需要 DB 迁移**(`archived_at` 已存在)。若 TDD 阶段发现需要加索引或字段,按 memory `feedback_prod_backward_compat` 的 4+4 条重新过一遍。

---

## 5 · 关联

- issue: https://github.com/LuoDi-Nate/financial-management/issues/12
- 分支: `issue/12-member-identity`
- 预览: `preview/v1.15/member-identity.html`
- 涉及既有代码:`repository/MemberMapper.java` · `service/AdminService.java` · `web/admin/AdminController.java` · `templates/admin/members.html` · `auth/SecurityConfig.java` · `auth/MemberPrincipalService.java` · `web/dashboard/DashboardController.java`(名字映射)· `web/account/AccountController.java` 与 `web/admin/NotificationSettingsController.java`(这两个是**确认不改**)
